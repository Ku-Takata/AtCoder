{
    "int_N": {
        "prefix": "nn",
        "body": [
            "${1:N} = int(input())"
        ],
        "description": "整数の入力"
    },

    "char_S": {
        "prefix": "ss",
        "body": [
            "${1:S} = input()"
        ],
        "description": "文字列の入力"
    },

    "int_N_M": {
        "prefix": "nm",
        "body": [
            "${1:N}, ${2:M} = map(int,input().split())"
        ],
        "description": "複数の整数の入力"
    },

    "int_N_M_L": {
        "prefix": "nml",
        "body": [
            "${1:N}, ${2:M}, ${3:L} = map(int,input().split())"
        ],
        "description": "複数の整数の入力"
    },

    "list_A": {
        "prefix": "ll",
        "body": [
            "${1:A} = list(map(${2:int},input().split()))"
        ],
        "description": "リストの入力"
    },

    "alphabet_list": {
        "prefix": "alphabet",
        "body": [
            "import string",
            "${1:alphabet} = list(string.ascii_lowercase)"
        ],
        "description": "アルファベット全列挙リスト"
    },

    "stack & que": {
        "prefix": "que",
        "body": [
            "from collections import deque",
            "import heapq",
            "Q = deque()"
        ],
        "description": "空のキューを作る"
    },

    "list_AB": {
        "prefix": "ll2",
        "body": [
            "${1:A} = [list(map(int,input().split())) for i in range(${2:N})]"
        ],
        "description": "2次元配列の入力"
    },

    "list_LR": {
        "prefix": "lr",
        "body": [
            "import sys",
            "",
            "n, *data = map(int, sys.stdin.read().split())",
            "L = data[0::2]",
            "R = data[1::2]"
        ],
        "description": "別々でリスト入力"
    },

    "list_sort": {
        "prefix": "ls",
        "body": [
            "${1:LR}.sort(key=lambda x: ${2:x[1]})"
        ],
        "description": "2次元配列の片方の値を見てソートする"
    },

    "for_i": {
        "prefix": "fori",
        "body": [
            "for ${1:i} in range(${2:N}):"
        ],
        "description": "for文"
    },

    "digit_list": {
        "prefix": "dl",
        "body": [
            "${1:A} = list(map(int, str(int(input()))))"
        ],
        "description": "整数をリスト化する"
    },

    "set_recusion_limit": {
        "prefix": "srl",
        "body": [
            "import sys",
            "sys.setrecursionlimit(10**7)"
        ],
        "description": "再帰回数上限の設定"
    },

    "move_news": {
        "prefix": "news",
        "body": [
            "for dy,dx in [[-1,0],[1,0],[0,-1],[0,1]]:",
            "    ny,nx = sy+dy, sx+dx",
            "    if 0 <= ny < H and 0 <= nx < W:"
        ],
        "description": "上下左右の移動が可能かどうかを判定"
    },

    "move_around": {
        "prefix": "around",
        "body": [
            "for dy,dx in [[-1,0],[-1,-1],[-1,1],[1,0],[1,-1],[1,1],[0,-1],[0,1]]:",
            "    ny,nx = sy+dy, sx+dx",
            "    if 0 <= ny < H and 0 <= nx < W:"
        ],
        "description": "斜め移動を含めた周りの移動が可能かどうかを判定"
    },

    "prime_number_enumeration": {
        "prefix": "pne",
        "body": [
            "import math",

            "# 0以上整数x「未満」の素数をリストに格納して返す",
            "def primes(x):",
            "    if x < 2: return []",

            "    primes = [i for i in range(x)]",
            "    primes[1] = 0",

            "    # エラトステネスのふるい",
            "    for prime in primes:",
            "        if prime > math.sqrt(x): break",
            "        if prime == 0: continue",
            "        for non_prime in range(2 * prime, x, prime): primes[non_prime] = 0",

            "    return [prime for prime in primes if prime != 0]"
        ]
    },

    "prime_number_judge": {
        "prefix": "pnj",
        "body": [
            "import math",

            "# 整数xが素数かどうかを判定する",
            "def is_prime(x):",
            "    if x < 2: return False # 2未満に素数はない",
            "    if x == 2 or x == 3 or x == 5: return True # 2,3,5は素数",
            "    if x % 2 == 0 or x % 3 == 0 or x % 5 == 0: return False # 2,3,5の倍数は合成数",

            "    # ためし割り: 疑似素数(2でも3でも5でも割り切れない数字)で次々に割っていく",
            "    prime = 7",
            "    step = 4",
            "    while prime <= math.sqrt(x):",
            "        if x % prime == 0: return False",

            "        prime += step",
            "        step = 6 - step",

            "    return True"
        ]
    },

    "Bipartite_Graph_Check(DFS)": {
		"prefix": "bipartite",
		"body": [
			"import sys",
			"sys.setrecursionlimit(10**7)",
			"",
			"def check_bipartite(N, graph):",
			"    \"\"\"",
			"    与えられたグラフが二部グラフかどうかを判定する",
			"    計算量: O(V + E)",
			"    \"\"\"",
			"    visited = [False] * N",
			"    color = [0] * N",
			"",
			"    def dfs(V, graph, visited, color, paint):",
			"        visited[V] = True",
			"        color[V] = paint",
			"",
			"        for E in graph[V]:",
			"            if not visited[E]:",
			"                if not dfs(E, graph, visited, color, -paint):",
			"                    return False",
			"            else:",
			"                if color[E] == color[V]:",
			"                    return False",
			"        return True",
			"",
			"    is_bipartite = True",
			"    for i in range(N):",
			"        if not visited[i]:",
			"            if not dfs(i, graph, visited, color, 1):",
			"                is_bipartite = False",
			"                break",
			"                ",
			"    return is_bipartite"
		],
		"description": "二部グラフかどうかをDFSで判定する"
	},

    "Create Graph (Adjacent List)": {
		"prefix": "graph",
		"body": [
            "import sys",
            "sys.setrecursionlimit(10**7)",
			"N, M = map(int, input().split())",
            "",
			"graph = [[] for i in range(N)]",
			"for i in range(M):",
			"    u, v = map(int, input().split())",
			"    u, v = u - 1, v - 1",
			"    graph[u].append(v)",
			"    graph[v].append(u)"
		],
		"description": "標準入力から頂点数N、辺数M、およびM本の辺を受け取り、隣接リスト形式のグラフを作成"
	},

    "DFS (Depth First Search) for Existing Graph": {
    "prefix": "dfs_basic",
    "body": [
        "import sys",
        "sys.setrecursionlimit(${1:10**7})",
        "",
        "visited = [False] * N",
        "",
        "def dfs(cur):",
        "    visited[cur] = True",
        "    $0",
        "    for next in graph[cur]:",
        "        if visited[next]:",
        "            continue",
        "        dfs(next)",
        "",
        "# dfs(${2:0})  # 頂点${2:0}から探索を開始する場合"
    ],
    "description": "作成済みの隣接リスト graph (頂点数N) に対してDFS（深さ優先探索）を実行"
    },

    "Count Edges in Cycles (NetworkX)": {
    "prefix": "nx_cycle_edges",
    "body": [
        "import sys",
        "import networkx as nx",
        "sys.setrecursionlimit(10**7)",
        "",
        "    # 入力の受け取り",
        "    ${1:N}, ${2:M} = map(int, input().split())",
        "    edges = []",
        "    for i in range(${2:M}):",
        "        u, v = map(int, input().split())",
        "        edges.append((u - 1, v - 1))",
        "",
        "    # グラフの構築",
        "    graph = nx.Graph()",
        "    graph.add_edges_from(edges)",
        "",
        "    # 閉路に含まれる辺の数 = 全体の辺の数 - 橋（どの閉路にも含まれない辺）の数",
        "    ans = ${2:M} - len(list(nx.bridges(graph)))",
        "    print(ans)",
        "    $0"
    ],
    "description": "NetworkXを使用して無向グラフの閉路に含まれる辺の数を計算"
    },

    "DFS: 経路復元 (バックトラック)": {
    "prefix": "dfs_path",
    "body": [
        "import sys",
        "sys.setrecursionlimit(10**7)",
        "",
        "visited = [False] * ${1:N}",
        "path = []",
        "",
        "def dfs(cur, goal):",
        "    visited[cur] = True",
        "    path.append(cur)",
        "    ",
        "    if cur == goal:",
        "        return True",
        "        ",
        "    for nxt in ${2:graph}[cur]:",
        "        if visited[nxt]:",
        "            continue",
        "        if dfs(nxt, goal):",
        "            return True",
        "            ",
        "    path.pop()",
        "    return False",
        "",
        "dfs(${3:start}, ${4:goal})"
    ],
    "description": "DFSによる経路復元（特定のゴールまでのパスを取得）"
    },

    "DFS: 木構造の探索 (親ノード記憶)": {
        "prefix": "dfs_tree",
        "body": [
        "import sys",
        "sys.setrecursionlimit(10**7)",
        "",
        "def dfs_tree(cur, pre=-1):",
        "    # 行きがけの処理",
        "    $3",
        "    ",
        "    for next in ${1:graph}[cur]:",
        "        if next == pre:",
        "            continue",
        "            ",
        "        dfs_tree(next, cur)",
        "        ",
        "        # 帰りがけの処理",
        "        $4",
        "",
        "dfs_tree(${2:0})"
        ],
        "description": "木構造に特化したDFS（visited配列不要、親ノードを引数で持つ）"
    },

    "メモ化再帰": {
        "prefix": "memo",
        "body": [
        "from functools import lru_cache",
        "",
        "@lru_cache(maxsize=1000)"
        ],
        "description": "メモ化再帰のおまじない"
    },

    "Recursive Function (Count/Total)": {
    "prefix": "rec_count",
    "body": [
        "import sys",
        "sys.setrecursionlimit(10**7)",
        "",
        "def ${1:solve}(${2:idx}, ${3:current_state}):",
        "    # 1. 終了条件（ベースケース）",
        "    if ${2:idx} == ${4:MAX_DEPTH}:",
        "        if ${3:current_state} == ${5:TARGET}:",
        "            return 1  # 条件達成なら1通り",
        "        else:",
        "            return 0  # 失敗なら0通り",
        "",
        "    # 2. 途中の処理（子ノードへの分岐と集計）",
        "    total = 0",
        "    # TODO: 選択肢のループをここに書く",
        "    # for next_choice in choices:",
        "    #     total += ${1:solve}(${2:idx} + 1, ${3:current_state} + next_choice)",
        "    ",
        "    return total",
        "",
        "ans = ${1:solve}(0, 0)",
        "print(ans)"
    ],
    "description": "再帰関数テンプレート（通り数・合計の数え上げ用）"
    },

    "Recursive Function (Yes/No Search)": {
    "prefix": "rec_check",
    "body": [
        "import sys",
        "sys.setrecursionlimit(10**7)",
        "",
        "def ${1:solve}(${2:idx}, ${3:current_state}):",
        "    # 1. 終了条件（ベースケース）",
        "    if ${2:idx} == ${4:MAX_DEPTH}:",
        "        return ${3:current_state} == ${5:TARGET}  # True か False を返す",
        "",
        "    # 2. 途中の処理（子ノードへの探索）",
        "    # TODO: 選択肢のループをここに書く",
        "    # for next_choice in choices:",
        "    #     if ${1:solve}(${2:idx} + 1, ${3:current_state} + next_choice):",
        "    #         return True  # 1つでもTrueが見つかったら即座に終了",
        "    ",
        "    return False  # 全て探索してダメならFalse",
        "",
        "if ${1:solve}(0, 0):",
        "    print(\"Yes\")",
        "else:",
        "    print(\"No\")"
    ],
    "description": "再帰関数テンプレート（条件を満たすものが存在するかどうか）"
    },

    "Segment Tree (セグメント木)": {
		"prefix": "segtree",
		"body": [
			"class SegmentTree:",
			"    def __init__(self, size, op, e):",
			"        self.n = size",
			"        self.op = op",
			"        self.e = e",
			"        self.size = 1 << (size - 1).bit_length()",
			"        self.tree = [e] * (2 * self.size)",
			"",
			"    def build(self, arr):",
			"        for i in range(self.n):",
			"            self.tree[self.size + i] = arr[i]",
			"        for i in range(self.size - 1, 0, -1):",
			"            self.tree[i] = self.op(self.tree[2 * i], self.tree[2 * i + 1])",
			"",
			"    def update(self, idx, val):",
			"        i = self.size + idx",
			"        self.tree[i] = val",
			"        while i > 1:",
			"            i >>= 1",
			"            self.tree[i] = self.op(self.tree[2 * i], self.tree[2 * i + 1])",
			"",
			"    def query(self, l, r):",
			"        s_left = self.e",
			"        s_right = self.e",
			"        l += self.size",
			"        r += self.size",
			"        while l < r:",
			"            if l & 1:",
			"                s_left = self.op(s_left, self.tree[l])",
			"                l += 1",
			"            if r & 1:",
			"                r -= 1",
			"                s_right = self.op(s_right, self.tree[r])",
			"            l >>= 1",
			"            r >>= 1",
			"        return self.op(s_left, s_right)",
			"",
			"# 区間最大値: st = SegmentTree(N, max, -float('inf'))",
			"# 区間最小値: st = SegmentTree(N, min, float('inf'))",
			"# 区間和:     st = SegmentTree(N, lambda x, y: x + y, 0)",
			"# 区間XOR:    st = SegmentTree(N, lambda x, y: x ^ y, 0)",
			"# st.build(A)"
		],
		"description": "非再帰版セグメント木 (点更新・区間取得: O(log N))"
	},

	"Fenwick Tree (BIT - セグ木互換仕様)": {
		"prefix": "bit",
		"body": [
			"class FenwickTree:",
			"    def __init__(self, size, is_xor=False):",
			"        self.n = size",
			"        self.tree = [0] * (size + 1)",
			"        self.raw = [0] * size",
			"        self.is_xor = is_xor",
			"",
			"    def build(self, arr):",
			"        \"\"\"初期配列から O(N) で構築\"\"\"",
			"        for i in range(self.n):",
			"            self.raw[i] = arr[i]",
			"            self.tree[i + 1] = arr[i]",
			"        for i in range(1, self.n + 1):",
			"            j = i + (i & -i)",
			"            if j <= self.n:",
			"                if self.is_xor:",
			"                    self.tree[j] ^= self.tree[i]",
			"                else:",
			"                    self.tree[j] += self.tree[i]",
			"",
			"    def add(self, idx, val):",
			"        \"\"\"A[idx] に val を加算 / XOR作用\"\"\"",
			"        if self.is_xor:",
			"            self.raw[idx] ^= val",
			"        else:",
			"            self.raw[idx] += val",
			"        i = idx + 1",
			"        while i <= self.n:",
			"            if self.is_xor:",
			"                self.tree[i] ^= val",
			"            else:",
			"                self.tree[i] += val",
			"            i += i & -i",
			"",
			"    def update(self, idx, val):",
			"        \"\"\"A[idx] = val に上書き更新 (セグ木と同仕様)\"\"\"",
			"        if self.is_xor:",
			"            diff = val ^ self.raw[idx]",
			"        else:",
			"            diff = val - self.raw[idx]",
			"        self.add(idx, diff)",
			"",
			"    def query(self, l, r):",
			"        \"\"\"半開区間 [l, r) の和/XORを取得 (セグ木と同仕様)\"\"\"",
			"        if self.is_xor:",
			"            return self._sum(r) ^ self._sum(l)",
			"        else:",
			"            return self._sum(r) - self._sum(l)",
			"",
			"    def _sum(self, idx):",
			"        s = 0",
			"        i = idx",
			"        while i > 0:",
			"            if self.is_xor:",
			"                s ^= self.tree[i]",
			"            else:",
			"                s += self.tree[i]",
			"            i -= i & -i",
			"        return s",
			"",
			"# 区間和 (デフォルト): bit = FenwickTree(N)",
			"# 区間XOR:            bit = FenwickTree(N, is_xor=True)",
			"# bit.build(A)"
		],
		"description": "セグ木とインターフェースを統一した Fenwick Tree (点加算/上書き・区間和/XOR)"
	},

    "AHC Simulated Annealing": {
		"prefix": "ahc-sa",
		"body": [
			"import sys",
			"import time",
			"import math",
			"",
			"def simulated_annealing():",
			"    # ローカル変数バインドによる高速化",
			"    perf_counter = time.perf_counter",
			"    exp = math.exp",
			"",
			"    start_time = perf_counter()",
			"    TIME_LIMIT = ${1:1.95}",
			"    T0 = ${2:2000.0}  # 開始温度",
			"    T1 = ${3:10.0}    # 終了温度",
			"    temp = T0",
			"    inv_temp = 1.0 / temp",
			"",
			"    # 高速乱数 (Xorshift64)",
			"    rng_x = 88172645463325252",
			"    def next_double():",
			"        nonlocal rng_x",
			"        rng_x ^= (rng_x << 13) & 0xFFFFFFFFFFFFFFFF",
			"        rng_x ^= (rng_x >> 7) & 0xFFFFFFFFFFFFFFFF",
			"        rng_x ^= (rng_x << 17) & 0xFFFFFFFFFFFFFFFF",
			"        return (rng_x >> 11) * (1.0 / (1 << 53))",
			"",
			"    # TODO: 初期状態の構築",
			"    # current_score = ...",
			"    # best_score = current_score",
			"",
			"    iter_count = 0",
			"    accept_count = 0",
			"    BATCH_SIZE = 256",
			"",
			"    while True:",
			"        # バッチごとの時間確認と温度更新",
			"        elapsed = perf_counter() - start_time",
			"        progress = elapsed / TIME_LIMIT",
			"        if progress >= 1.0:",
			"            break",
			"        ",
			"        # 線形補間と逆数キャッシュ（割り算を乗算に変換）",
			"        temp = T0 + (T1 - T0) * progress",
			"        inv_temp = 1.0 / temp",
			"",
			"        for _ in range(BATCH_SIZE):",
			"            # TODO: 近傍操作と差分計算",
			"            # diff = ...  # 最大化: new_score - current_score",
			"",
			"            # 遷移判定 (最大化問題の例)",
			"            # if diff >= 0 or (diff * inv_temp > -10.0 and exp(diff * inv_temp) > next_double()):",
			"            #     current_score += diff",
			"            #     accept_count += 1",
			"            #     if current_score > best_score:",
			"            #         best_score = current_score",
			"            #         # best_stateの退避 (deepcopyは避け、差分やスライスを使用)",
			"            # else:",
			"            #     # TODO: 状態のロールバック",
			"            pass",
			"",
			"        iter_count += BATCH_SIZE",
			"",
			"    # デバッグ出力",
			"    total_time_ms = (perf_counter() - start_time) * 1000",
			"    ratio = (accept_count / iter_count * 100) if iter_count > 0 else 0.0",
			"    print(f'[DEBUG] === Simulated Annealing ===', file=sys.stderr)",
			"    print(f'[DEBUG] Iterations   : {iter_count}', file=sys.stderr)",
			"    print(f'[DEBUG] Accept Ratio : {ratio:.2f}% ({accept_count}/{iter_count})', file=sys.stderr)",
			"    print(f'[DEBUG] Elapsed Time : {total_time_ms:.2f} ms', file=sys.stderr)",
			"    # print(f'[DEBUG] Best Score   : {best_score}', file=sys.stderr)",
			"    print(f'[DEBUG] =============================', file=sys.stderr)",
			"",
			"    # return best_state"
		],
		"description": "AHC Heavily-Optimized Simulated Annealing for PyPy"
	},

	"AHC Hill Climbing": {
		"prefix": "ahc-hc",
		"body": [
			"import sys",
			"import time",
			"",
			"def hill_climbing():",
			"    perf_counter = time.perf_counter",
			"    start_time = perf_counter()",
			"    TIME_LIMIT = ${1:1.95}",
			"",
			"    # TODO: 初期状態の構築",
			"    # current_score = ...",
			"    # best_score = current_score",
			"",
			"    iter_count = 0",
			"    update_count = 0",
			"    BATCH_SIZE = 256",
			"",
			"    while True:",
			"        if perf_counter() - start_time >= TIME_LIMIT:",
			"            break",
			"",
			"        for _ in range(BATCH_SIZE):",
			"            # TODO: 近傍操作と差分計算",
			"            # diff = ...  # 最大化: new_score - current_score",
			"",
			"            # 改善時のみ採択 (最大化問題の例)",
			"            # if diff > 0:",
			"            #     current_score += diff",
			"            #     best_score = current_score",
			"            #     update_count += 1",
			"            #     # best_stateの退避",
			"            # else:",
			"            #     # TODO: 状態のロールバック",
			"            pass",
			"",
			"        iter_count += BATCH_SIZE",
			"",
			"    # デバッグ出力",
			"    total_time_ms = (perf_counter() - start_time) * 1000",
			"    ratio = (update_count / iter_count * 100) if iter_count > 0 else 0.0",
			"    print(f'[DEBUG] === Hill Climbing ===', file=sys.stderr)",
			"    print(f'[DEBUG] Iterations   : {iter_count}', file=sys.stderr)",
			"    print(f'[DEBUG] Updates      : {update_count} ({ratio:.2f}%)', file=sys.stderr)",
			"    print(f'[DEBUG] Elapsed Time : {total_time_ms:.2f} ms', file=sys.stderr)",
			"    # print(f'[DEBUG] Best Score   : {best_score}', file=sys.stderr)",
			"    print(f'[DEBUG] =====================', file=sys.stderr)",
			"",
			"    # return best_state"
		],
		"description": "AHC Heavily-Optimized Hill Climbing for PyPy"
	},

    "AHC Beam Search with Zobrist Hash": {
		"prefix": "ahc-bs-zobrist",
		"body": [
			"import sys",
			"import time",
			"import random",
			"",
			"def beam_search():",
			"    perf_counter = time.perf_counter",
			"    start_time = perf_counter()",
			"    TIME_LIMIT = ${1:1.95}",
			"    BEAM_WIDTH = ${2:100}",
			"    MAX_DEPTH = ${3:50}",
			"",
			"    # 親ポインタ木 (parent_id, action)",
			"    tree_nodes = [(-1, None)]",
			"",
			"    # --- Zobrist Hash テーブル初期化 ---",
			"    # TODO: 状態表現に応じた64bit整数テーブルを作成",
			"    # rng = random.Random(42)",
			"    # ZOBRIST_TABLE = [[rng.getrandbits(64) for _ in range(NUM_STATES)] for _ in range(BOARD_SIZE)]",
			"",
			"    # 要素: (eval_score, state, node_id, current_hash)",
			"    # TODO: 初期状態の構築と初期ハッシュ計算",
			"    # init_state = ...",
			"    # init_score = 0",
			"    # init_hash = 0",
			"    # current_beam = [(init_score, init_state, 0, init_hash)]",
			"",
			"    best_score = -float('inf')",
			"    best_node_id = -1",
			"    reached_depth = 0",
			"    total_generated = 0",
			"    total_pruned = 0",
			"",
			"    for depth in range(MAX_DEPTH):",
			"        if perf_counter() - start_time >= TIME_LIMIT:",
			"            break",
			"        reached_depth = depth + 1",
			"",
			"        next_candidates = []",
			"        # 深さごとに合流を検知する訪問済みセット (Python set のハッシュ検索はint型で最速)",
			"        visited_hashes = set()",
			"        visited_add = visited_hashes.add",
			"",
			"        # ビーム内の各状態を展開",
			"        # for score, state, node_id, current_hash in current_beam:",
			"        #     for op in get_actions(state):",
			"        #         total_generated += 1",
			"        #         # 差分ハッシュ計算 (例: XOR更新)",
			"        #         next_hash = current_hash ^ ZOBRIST_TABLE[op.pos][op.val]",
			"        #         if next_hash in visited_hashes:",
			"        #             total_pruned += 1",
			"        #             continue",
			"        #         visited_add(next_hash)",
			"        #         ",
			"        #         next_state = apply_action(state, op)",
			"        #         next_score = calc_score(next_state)",
			"        #         ",
			"        #         tree_nodes.append((node_id, op))",
			"        #         new_node_id = len(tree_nodes) - 1",
			"        #         next_candidates.append((next_score, next_state, new_node_id, next_hash))",
			"",
			"        if not next_candidates:",
			"            break",
			"",
			"        # スコアのみで降順ソート",
			"        next_candidates.sort(key=lambda x: x[0], reverse=True)",
			"        current_beam = next_candidates[:BEAM_WIDTH]",
			"",
			"        if current_beam[0][0] > best_score:",
			"            best_score = current_beam[0][0]",
			"            best_node_id = current_beam[0][2]",
			"",
			"    # 操作列復元",
			"    actions = []",
			"    curr = best_node_id",
			"    while curr > 0:",
			"        parent_id, act = tree_nodes[curr]",
			"        actions.append(act)",
			"        curr = parent_id",
			"    actions.reverse()",
			"",
			"    # デバッグ出力",
			"    total_time_ms = (perf_counter() - start_time) * 1000",
			"    prune_ratio = (total_pruned / total_generated * 100.0) if total_generated > 0 else 0.0",
			"    print(f'[DEBUG] === Beam Search (Zobrist Hash) ===', file=sys.stderr)",
			"    print(f'[DEBUG] Best Score       : {best_score}', file=sys.stderr)",
			"    print(f'[DEBUG] Reached Depth    : {reached_depth}/{MAX_DEPTH}', file=sys.stderr)",
			"    print(f'[DEBUG] Pruned by Hash   : {total_pruned}/{total_generated} ({prune_ratio:.2f}%)', file=sys.stderr)",
			"    print(f'[DEBUG] Tree Nodes Size  : {len(tree_nodes)}', file=sys.stderr)",
			"    print(f'[DEBUG] Elapsed Time     : {total_time_ms:.2f} ms', file=sys.stderr)",
			"    print(f'[DEBUG] =====================================', file=sys.stderr)",
			"",
			"    # return actions"
		],
		"description": "AHC Optimized Beam Search with Zobrist Hash Pruning for PyPy"
	},

	"AHC Chokudai Search with Zobrist Hash": {
		"prefix": "ahc-cs-zobrist",
		"body": [
			"import sys",
			"import time",
			"import heapq",
			"import random",
			"",
			"def chokudai_search():",
			"    perf_counter = time.perf_counter",
			"    heappush = heapq.heappush",
			"    heappop = heapq.heappop",
			"",
			"    start_time = perf_counter()",
			"    TIME_LIMIT = ${1:1.95}",
			"    CHOKUDAI_WIDTH = ${2:1}",
			"    MAX_DEPTH = ${3:50}",
			"",
			"    # 親ポインタ木 (parent_id, action)",
			"    tree_nodes = [(-1, None)]",
			"",
			"    # --- Zobrist Hash テーブル初期化 ---",
			"    # rng = random.Random(42)",
			"    # ZOBRIST_TABLE = [[rng.getrandbits(64) for _ in range(NUM_STATES)] for _ in range(BOARD_SIZE)]",
			"",
			"    # 深さごとの優先度キューと訪問済みハッシュ集合",
			"    # キュー要素: (-score, tie_breaker, state, node_id, current_hash)",
			"    beams = [[] for _ in range(MAX_DEPTH + 1)]",
			"    visited_hashes = [set() for _ in range(MAX_DEPTH + 1)]",
			"",
			"    # TODO: 初期状態の構築",
			"    # init_state = ...",
			"    # init_score = 0",
			"    # init_hash = 0",
			"    # heappush(beams[0], (-init_score, 0, init_state, 0, init_hash))",
			"    # visited_hashes[0].add(init_hash)",
			"",
			"    tie_breaker = 1",
			"    round_count = 0",
			"    total_generated = 0",
			"    total_pruned = 0",
			"    best_score = -float('inf')",
			"    best_node_id = -1",
			"",
			"    while True:",
			"        if perf_counter() - start_time >= TIME_LIMIT:",
			"            break",
			"        round_count += 1",
			"",
			"        for d in range(MAX_DEPTH):",
			"            beam = beams[d]",
			"            next_beam = beams[d + 1]",
			"            next_visited = visited_hashes[d + 1]",
			"            next_visited_add = next_visited.add",
			"",
			"            for _ in range(CHOKUDAI_WIDTH):",
			"                if not beam:",
			"                    break",
			"                neg_score, _, state, node_id, current_hash = heappop(beam)",
			"",
			"                # TODO: 次状態の展開",
			"                # for op in get_actions(state):",
			"                #     total_generated += 1",
			"                #     next_hash = current_hash ^ ZOBRIST_TABLE[op.pos][op.val]",
			"                #     if next_hash in next_visited:",
			"                #         total_pruned += 1",
			"                #         continue",
			"                #     next_visited_add(next_hash)",
			"                #     ",
			"                #     next_state = apply_action(state, op)",
			"                #     next_score = calc_score(next_state)",
			"                #     ",
			"                #     tree_nodes.append((node_id, op))",
			"                #     new_node_id = len(tree_nodes) - 1",
			"                #     heappush(next_beam, (-next_score, tie_breaker, next_state, new_node_id, next_hash))",
			"                #     tie_breaker += 1",
			"                #     ",
			"                #     if d + 1 == MAX_DEPTH and next_score > best_score:",
			"                #         best_score = next_score",
			"                #         best_node_id = new_node_id",
			"                pass",
			"",
			"    # 操作列復元",
			"    actions = []",
			"    curr = best_node_id",
			"    while curr > 0:",
			"        parent_id, act = tree_nodes[curr]",
			"        actions.append(act)",
			"        curr = parent_id",
			"    actions.reverse()",
			"",
			"    # デバッグ出力",
			"    total_time_ms = (perf_counter() - start_time) * 1000",
			"    prune_ratio = (total_pruned / total_generated * 100.0) if total_generated > 0 else 0.0",
			"    print(f'[DEBUG] === Chokudai Search (Zobrist Hash) ===', file=sys.stderr)",
			"    print(f'[DEBUG] Rounds Completed : {round_count}', file=sys.stderr)",
			"    print(f'[DEBUG] Best Score       : {best_score}', file=sys.stderr)",
			"    print(f'[DEBUG] Pruned by Hash   : {total_pruned}/{total_generated} ({prune_ratio:.2f}%)', file=sys.stderr)",
			"    print(f'[DEBUG] Goal Candidates  : {len(beams[MAX_DEPTH])}', file=sys.stderr)",
			"    print(f'[DEBUG] Tree Nodes Size  : {len(tree_nodes)}', file=sys.stderr)",
			"    print(f'[DEBUG] Elapsed Time     : {total_time_ms:.2f} ms', file=sys.stderr)",
			"    print(f'[DEBUG] ========================================', file=sys.stderr)",
			"",
			"    # return actions"
		],
		"description": "AHC Optimized Chokudai Search with Zobrist Hash Pruning for PyPy"
	},

    "AHC A* and Dijkstra": {
		"prefix": "ahc-astar",
		"body": [
			"import sys",
			"import time",
			"import heapq",
			"",
			"def astar_search(num_nodes, start, goal, get_neighbors, heuristic=None):",
			"    \"\"\"",
			"    num_nodes: 頂点数 (0-indexed)",
			"    start, goal: 始点・終点の頂点ID",
			"    get_neighbors: u -> Iterable[(next_u, cost)]",
			"    heuristic: (u, goal) -> 推定残りコスト (Noneならダイクストラ法として動作)",
			"    \"\"\"",
			"    perf_counter = time.perf_counter",
			"    heappush = heapq.heappush",
			"    heappop = heapq.heappop",
			"    start_time = perf_counter()",
			"",
			"    if heuristic is None:",
			"        h = lambda u, g: 0",
			"    else:",
			"        h = heuristic",
			"",
			"    INF = 10**18",
			"    dist = [INF] * num_nodes",
			"    parent = [-1] * num_nodes",
			"",
			"    dist[start] = 0",
			"    # 要素: (f_score, g_score, u)",
			"    pq = [(h(start, goal), 0, start)]",
			"    expanded_nodes = 0",
			"",
			"    found = False",
			"    while pq:",
			"        f, g, u = heappop(pq)",
			"        if g > dist[u]:",
			"            continue",
			"        expanded_nodes += 1",
			"",
			"        if u == goal:",
			"            found = True",
			"            break",
			"",
			"        for v, cost in get_neighbors(u):",
			"            new_g = g + cost",
			"            if new_g < dist[v]:",
			"                dist[v] = new_g",
			"                parent[v] = u",
			"                heappush(pq, (new_g + h(v, goal), new_g, v))",
			"",
			"    # 経路復元",
			"    path = []",
			"    if found:",
			"        curr = goal",
			"        while curr != -1:",
			"            path.append(curr)",
			"            curr = parent[curr]",
			"        path.reverse()",
			"",
			"    # デバッグ出力",
			"    elapsed_ms = (perf_counter() - start_time) * 1000",
			"    print(f'[DEBUG] === A* / Dijkstra ===', file=sys.stderr)",
			"    print(f'[DEBUG] Path Found     : {found}', file=sys.stderr)",
			"    print(f'[DEBUG] Shortest Cost  : {dist[goal] if found else \"INF\"}', file=sys.stderr)",
			"    print(f'[DEBUG] Path Length    : {len(path)}', file=sys.stderr)",
			"    print(f'[DEBUG] Expanded Nodes : {expanded_nodes}', file=sys.stderr)",
			"    print(f'[DEBUG] Elapsed Time   : {elapsed_ms:.3f} ms', file=sys.stderr)",
			"    print(f'[DEBUG] =====================', file=sys.stderr)",
			"",
			"    return (dist[goal], path) if found else (INF, [])"
		],
		"description": "AHC Optimized A* and Dijkstra Algorithm for PyPy"
	},

	"AHC Rollback Union-Find": {
		"prefix": "ahc-uf-rollback",
		"body": [
			"class RollbackUnionFind:",
			"    \"\"\"",
			"    焼きなまし法の近傍評価・棄却に対応した、巻き戻し機能付き素集合データ構造。",
			"    ※ 経路圧縮を行わず Union by Size のみを用いるため O(log N) で完全な Undo が可能。",
			"    \"\"\"",
			"    __slots__ = ('parent_or_size', 'history')",
			"",
			"    def __init__(self, n):",
			"        # 根の場合は -(グループサイズ)、子の場合は親のインデックス",
			"        self.parent_or_size = [-1] * n",
			"        self.history = []",
			"",
			"    def leader(self, a):",
			"        curr = a",
			"        pos = self.parent_or_size",
			"        while pos[curr] >= 0:",
			"            curr = pos[curr]",
			"        return curr",
			"",
			"    def same(self, a, b):",
			"        return self.leader(a) == self.leader(b)",
			"",
			"    def size(self, a):",
			"        return -self.parent_or_size[self.leader(a)]",
			"",
			"    def merge(self, a, b):",
			"        x = self.leader(a)",
			"        y = self.leader(b)",
			"        if x == y:",
			"            self.history.append((-1, 0, -1, 0))",
			"            return False",
			"",
			"        pos = self.parent_or_size",
			"        # 小さい木 (x) を大きい木 (y) にマージ",
			"        if -pos[x] < -pos[y]:",
			"            x, y = y, x",
			"",
			"        # 変更前の状態を履歴にプッシュ: (x, pos[x], y, pos[y])",
			"        self.history.append((x, pos[x], y, pos[y]))",
			"        pos[x] += pos[y]",
			"        pos[y] = x",
			"        return True",
			"",
			"    def undo(self):",
			"        \"\"\"直前の merge を1回分取り消す\"\"\"",
			"        if not self.history:",
			"            return",
			"        x, px, y, py = self.history.pop()",
			"        if x == -1:",
			"            return",
			"        self.parent_or_size[x] = px",
			"        self.parent_or_size[y] = py",
			"",
			"    def snapshot(self):",
			"        \"\"\"現在の変更時点のトークンを取得\"\"\"",
			"        return len(self.history)",
			"",
			"    def rollback(self, snap):",
			"        \"\"\"指定したスナップショット時点まで全て巻き戻す\"\"\"",
			"        while len(self.history) > snap:",
			"            self.undo()"
		],
		"description": "AHC Undoable (Rollback) Union-Find for PyPy"
	},

	"AHC Multi-Start Local Search": {
		"prefix": "ahc-multistart",
		"body": [
			"import sys",
			"import time",
			"import random",
			"",
			"def multi_start_search():",
			"    perf_counter = time.perf_counter",
			"    start_time = perf_counter()",
			"    TIME_LIMIT = ${1:1.95}",
			"    STEP_LIMIT_PER_START = ${2:500}  # 1回のスタートあたりの局所探索ステップ上限",
			"",
			"    # 高速乱数 (Xorshift64)",
			"    rng_x = 88172645463325252",
			"    def next_int(l, r):",
			"        nonlocal rng_x",
			"        rng_x ^= (rng_x << 13) & 0xFFFFFFFFFFFFFFFF",
			"        rng_x ^= (rng_x >> 7) & 0xFFFFFFFFFFFFFFFF",
			"        rng_x ^= (rng_x << 17) & 0xFFFFFFFFFFFFFFFF",
			"        return l + (rng_x % (r - l + 1))",
			"",
			"    best_score = -float('inf')",
			"    best_state = None",
			"    starts_count = 0",
			"    total_local_steps = 0",
			"",
			"    while True:",
			"        if perf_counter() - start_time >= TIME_LIMIT:",
			"            break",
			"        starts_count += 1",
			"",
			"        # --- 1. 多様性を持たせた初期解の生成 (ランダム貪欲等) ---",
			"        # state = generate_random_greedy_state(seed=next_int(0, 10**9))",
			"        # score = evaluate(state)",
			"",
			"        # --- 2. 短時間の局所探索 (山登り) ---",
			"        for step in range(STEP_LIMIT_PER_START):",
			"            # TODO: 近傍操作と差分更新",
			"            # diff = ...",
			"            # if diff > 0:",
			"            #     score += diff",
			"            #     # 状態の適用",
			"            # else:",
			"            #     # ロールバック",
			"            pass",
			"        total_local_steps += STEP_LIMIT_PER_START",
			"",
			"        # --- 3. グローバル最良解の更新 ---",
			"        # if score > best_score:",
			"        #     best_score = score",
			"        #     best_state = state[:]  # 浅いコピーまたは複製",
			"",
			"    # デバッグ出力",
			"    total_time_ms = (perf_counter() - start_time) * 1000",
			"    avg_steps = total_local_steps // starts_count if starts_count > 0 else 0",
			"    print(f'[DEBUG] === Multi-Start Search ===', file=sys.stderr)",
			"    print(f'[DEBUG] Starts Executed : {starts_count}', file=sys.stderr)",
			"    print(f'[DEBUG] Total Steps     : {total_local_steps} (avg: {avg_steps}/start)', file=sys.stderr)",
			"    print(f'[DEBUG] Best Score      : {best_score}', file=sys.stderr)",
			"    print(f'[DEBUG] Elapsed Time    : {total_time_ms:.2f} ms', file=sys.stderr)",
			"    print(f'[DEBUG] =========================', file=sys.stderr)",
			"",
			"    # return best_state"
		],
		"description": "AHC Heavily-Optimized Multi-Start Local Search for PyPy"
	},

    "AHC 2-opt": {
		"prefix": "ahc-2opt",
		"body": [
			"import sys",
			"import time",
			"",
			"def two_opt(n, initial_tour, get_dist, time_limit=1.95):",
			"    \"\"\"",
			"    n: 頂点数",
			"    initial_tour: 0〜n-1 の順列 (リスト)",
			"    get_dist: (u, v) -> コスト/距離",
			"    \"\"\"",
			"    perf_counter = time.perf_counter",
			"    start_time = perf_counter()",
			"",
			"    tour = initial_tour[:]",
			"    # コストテーブルを事前計算可能な場合は配列化を推奨",
			"    # dist = [[get_dist(i, j) for j in range(n)] for i in range(n)]",
			"",
			"    improved = True",
			"    iter_count = 0",
			"    update_count = 0",
			"",
			"    while improved:",
			"        improved = False",
			"        for i in range(n - 2):",
			"            if (iter_count & 511) == 0 and (perf_counter() - start_time >= time_limit):",
			"                break",
			"",
			"            u1 = tour[i]",
			"            u2 = tour[i + 1]",
			"            d_u1_u2 = get_dist(u1, u2)",
			"",
			"            for j in range(i + 2, n if i > 0 else n - 1):",
			"                iter_count += 1",
			"                v1 = tour[j]",
			"                v2 = tour[(j + 1) % n]",
			"",
			"                # 辺 (u1, u2), (v1, v2) を (u1, v1), (u2, v2) に繋ぎ変える差分コスト",
			"                diff = (get_dist(u1, v1) + get_dist(u2, v2)) - (d_u1_u2 + get_dist(v1, v2))",
			"",
			"                if diff < 0:  # 最小化で改善",
			"                    tour[i + 1 : j + 1] = tour[i + 1 : j + 1][::-1]",
			"                    update_count += 1",
			"                    improved = True",
			"                    break  # First Improvement (即座に適用して次へ)",
			"            if improved:",
			"                break",
			"",
			"        if perf_counter() - start_time >= time_limit:",
			"            break",
			"",
			"    # 最終総コストの計算",
			"    total_cost = sum(get_dist(tour[k], tour[(k + 1) % n]) for k in range(n))",
			"",
			"    # デバッグ出力",
			"    elapsed_ms = (perf_counter() - start_time) * 1000",
			"    print(f'[DEBUG] === 2-opt ===', file=sys.stderr)",
			"    print(f'[DEBUG] Final Cost   : {total_cost}', file=sys.stderr)",
			"    print(f'[DEBUG] Updates      : {update_count}', file=sys.stderr)",
			"    print(f'[DEBUG] Iterations   : {iter_count}', file=sys.stderr)",
			"    print(f'[DEBUG] Elapsed Time : {elapsed_ms:.2f} ms', file=sys.stderr)",
			"    print(f'[DEBUG] =============', file=sys.stderr)",
			"",
			"    return tour, total_cost"
		],
		"description": "AHC Fast 2-opt Local Search for TSP / Permutation for PyPy"
	},

	"AHC 2D Cumulative Sum and Imos": {
		"prefix": "ahc-2d-grid",
		"body": [
			"class Grid2D:",
			"    \"\"\"",
			"    2次元累積和および2次元いもす法 (H x W)。",
			"    PyPyでの速度を最大化するため、内部は1次元配列フラット管理。",
			"    \"\"\"",
			"    __slots__ = ('h', 'w', 'data', 'pref')",
			"",
			"    def __init__(self, h, w):",
			"        self.h = h",
			"        self.w = w",
			"        self.data = [0] * (h * w)",
			"        self.pref = None",
			"",
			"    # --- 2次元いもす法 (矩形加算) ---",
			"    def add_rect(self, r1, c1, r2, c2, val):",
			"        \"\"\"半開区間 [r1, r2) x [c1, c2) に val を加算 (蓄積)\"\"\"",
			"        w = self.w",
			"        data = self.data",
			"        data[r1 * w + c1] += val",
			"        if c2 < w:",
			"            data[r1 * w + c2] -= val",
			"        if r2 < self.h:",
			"            data[r2 * w + c1] -= val",
			"            if c2 < w:",
			"                data[r2 * w + c2] += val",
			"",
			"    def build_imos(self):",
			"        \"\"\"いもす法の累積加算を適用してグリッドを復元 O(HW)\"\"\"",
			"        h, w = self.h, self.w",
			"        data = self.data",
			"        # 横方向の累積和",
			"        for r in range(h):",
			"            row_offset = r * w",
			"            for c in range(1, w):",
			"                data[row_offset + c] += data[row_offset + c - 1]",
			"        # 縦方向の累積和",
			"        for r in range(1, h):",
			"            row_offset = r * w",
			"            prev_offset = (r - 1) * w",
			"            for c in range(w):",
			"                data[row_offset + c] += data[prev_offset + c]",
			"",
			"    # --- 2次元累積和 (矩形クエリ O(1)) ---",
			"    def build_prefix_sum(self):",
			"        \"\"\"現在の data に基づいて (H+1) x (W+1) の累積和配列を構築 O(HW)\"\"\"",
			"        h, w = self.h, self.w",
			"        pw = w + 1",
			"        pref = [0] * ((h + 1) * pw)",
			"        data = self.data",
			"",
			"        for r in range(h):",
			"            data_offset = r * w",
			"            pref_curr = (r + 1) * pw",
			"            pref_prev = r * pw",
			"            for c in range(w):",
			"                pref[pref_curr + (c + 1)] = (",
			"                    data[data_offset + c]",
			"                    + pref[pref_prev + (c + 1)]",
			"                    + pref[pref_curr + c]",
			"                    - pref[pref_prev + c]",
			"                )",
			"        self.pref = pref",
			"",
			"    def query_rect(self, r1, c1, r2, c2):",
			"        \"\"\"半開区間 [r1, r2) x [c1, c2) の総和を O(1) で取得\"\"\"",
			"        pw = self.w + 1",
			"        pref = self.pref",
			"        return (",
			"            pref[r2 * pw + c2]",
			"            - pref[r1 * pw + c2]",
			"            - pref[r2 * pw + c1]",
			"            + pref[r1 * pw + c1]",
			"        )"
		],
		"description": "AHC Optimized 2D Prefix Sum and Imos Algorithm for PyPy"
	},

	"AHC Kruskal (MST)": {
		"prefix": "ahc-kruskal",
		"body": [
			"import sys",
			"import time",
			"",
			"def kruskal(num_nodes, edges):",
			"    \"\"\"",
			"    num_nodes: 頂点数 (0-indexed)",
			"    edges: [(u, v, cost), ...] のリスト",
			"    戻り値: (mst_cost, mst_edges)",
			"    \"\"\"",
			"    perf_counter = time.perf_counter",
			"    start_time = perf_counter()",
			"",
			"    # コスト昇順ソート",
			"    edges.sort(key=lambda e: e[2])",
			"",
			"    # 高速 Union-Find (経路圧縮 + ランク)",
			"    parent = list(range(num_nodes))",
			"    rank = [0] * num_nodes",
			"",
			"    def find(i):",
			"        path = []",
			"        while parent[i] != i:",
			"            path.append(i)",
			"            i = parent[i]",
			"        for node in path:",
			"            parent[node] = i",
			"        return i",
			"",
			"    def union(i, j):",
			"        root_i = find(i)",
			"        root_j = find(j)",
			"        if root_i == root_j:",
			"            return False",
			"        if rank[root_i] < rank[root_j]:",
			"            root_i, root_j = root_j, root_i",
			"        parent[root_j] = root_i",
			"        if rank[root_i] == rank[root_j]:",
			"            rank[root_i] += 1",
			"        return True",
			"",
			"    mst_cost = 0",
			"    mst_edges = []",
			"",
			"    for u, v, cost in edges:",
			"        if union(u, v):",
			"            mst_cost += cost",
			"            mst_edges.append((u, v, cost))",
			"            if len(mst_edges) == num_nodes - 1:",
			"                break",
			"",
			"    is_connected = (len(mst_edges) == num_nodes - 1) if num_nodes > 1 else True",
			"",
			"    # デバッグ出力",
			"    elapsed_ms = (perf_counter() - start_time) * 1000",
			"    print(f'[DEBUG] === Kruskal MST ===', file=sys.stderr)",
			"    print(f'[DEBUG] MST Cost     : {mst_cost}', file=sys.stderr)",
			"    print(f'[DEBUG] Edge Count   : {len(mst_edges)}/{num_nodes - 1}', file=sys.stderr)",
			"    print(f'[DEBUG] Connected    : {is_connected}', file=sys.stderr)",
			"    print(f'[DEBUG] Elapsed Time : {elapsed_ms:.2f} ms', file=sys.stderr)",
			"    print(f'[DEBUG] ===================', file=sys.stderr)",
			"",
			"    return mst_cost, mst_edges"
		],
		"description": "AHC Optimized Kruskal Minimum Spanning Tree for PyPy"
	},

    "AHC Fast BFS / 01-BFS": {
		"prefix": "ahc-bfs",
		"body": [
			"import sys",
			"import time",
			"from collections import deque",
			"",
			"def grid_bfs(h, w, starts, is_passable, get_cost=None):",
			"    \"\"\"",
			"    h, w: グリッドの高さと幅",
			"    starts: [(r, c), ...] 始点リスト (多始点BFS対応)",
			"    is_passable: (r, c) -> bool 通行可能判定",
			"    get_cost: (r1, c1, r2, c2) -> 0 or 1 (Noneなら重み1の通常BFS)",
			"    \"\"\"",
			"    perf_counter = time.perf_counter",
			"    start_time = perf_counter()",
			"",
			"    INF = 10**9",
			"    dist = [INF] * (h * w)",
			"    q = deque()",
			"    q_append = q.append",
			"    q_appendleft = q.appendleft",
			"    q_popleft = q.popleft",
			"",
			"    for sr, sc in starts:",
			"        idx = sr * w + sc",
			"        dist[idx] = 0",
			"        q_append(idx)",
			"",
			"    visited_count = 0",
			"    DR = (-1, 1, 0, 0)",
			"    DC = (0, 0, -1, 1)",
			"",
			"    if get_cost is None:",
			"        # 通常 BFS (コスト一様: 1)",
			"        while q:",
			"            curr = q_popleft()",
			"            d = dist[curr]",
			"            visited_count += 1",
			"            r = curr // w",
			"            c = curr % w",
			"",
			"            for i in range(4):",
			"                nr = r + DR[i]",
			"                nc = c + DC[i]",
			"                if 0 <= nr < h and 0 <= nc < w and is_passable(nr, nc):",
			"                    nidx = nr * w + nc",
			"                    if dist[nidx] == INF:",
			"                        dist[nidx] = d + 1",
			"                        q_append(nidx)",
			"    else:",
			"        # 01-BFS",
			"        while q:",
			"            curr = q_popleft()",
			"            d = dist[curr]",
			"            visited_count += 1",
			"            r = curr // w",
			"            c = curr % w",
			"",
			"            for i in range(4):",
			"                nr = r + DR[i]",
			"                nc = c + DC[i]",
			"                if 0 <= nr < h and 0 <= nc < w and is_passable(nr, nc):",
			"                    nidx = nr * w + nc",
			"                    cost = get_cost(r, c, nr, nc)",
			"                    nd = d + cost",
			"                    if nd < dist[nidx]:",
			"                        dist[nidx] = nd",
			"                        if cost == 0:",
			"                            q_appendleft(nidx)",
			"                        else:",
			"                            q_append(nidx)",
			"",
			"    # デバッグ出力",
			"    elapsed_ms = (perf_counter() - start_time) * 1000",
			"    print(f'[DEBUG] === Grid BFS ===', file=sys.stderr)",
			"    print(f'[DEBUG] Reached Cells : {visited_count}/{h * w}', file=sys.stderr)",
			"    print(f'[DEBUG] Elapsed Time  : {elapsed_ms:.2f} ms', file=sys.stderr)",
			"    print(f'[DEBUG] ================', file=sys.stderr)",
			"",
			"    return dist"
		],
		"description": "AHC Fast Grid BFS / 01-BFS with multi-start support for PyPy"
	},

	"AHC 2D Spatial Bucket Index": {
		"prefix": "ahc-spatial-bucket",
		"body": [
			"class SpatialBucket2D:",
			"    \"\"\"",
			"    2次元座標上の点を空間分割し、最近傍点や範囲内アイテムを高速検索するインデックス。",
			"    \"\"\"",
			"    __slots__ = ('bucket_size', 'inv_bucket_size', 'buckets')",
			"",
			"    def __init__(self, bucket_size):",
			"        self.bucket_size = bucket_size",
			"        self.inv_bucket_size = 1.0 / bucket_size",
			"        self.buckets = {}  # (bx, by) -> list of (point_id, x, y)",
			"",
			"    def clear(self):",
			"        self.buckets.clear()",
			"",
			"    def add(self, point_id, x, y):",
			"        bx = int(x * self.inv_bucket_size)",
			"        by = int(y * self.inv_bucket_size)",
			"        key = (bx, by)",
			"        if key in self.buckets:",
			"            self.buckets[key].append((point_id, x, y))",
			"        else:",
			"            self.buckets[key] = [(point_id, x, y)]",
			"",
			"    def find_nearest(self, qx, qy):",
			"        \"\"\"クエリ点 (qx, qy) に最も近い点 (point_id, x, y, dist_sq) を探索\"\"\"",
			"        bx = int(qx * self.inv_bucket_size)",
			"        by = int(qy * self.inv_bucket_size)",
			"        best_dist_sq = 10**18",
			"        best_item = None",
			"        buckets = self.buckets",
			"",
			"        # 周囲のバケットを渦巻き状に探索 (半径 expand を広げる)",
			"        expand = 0",
			"        while True:",
			"            found_in_ring = False",
			"            for dx in range(-expand, expand + 1):",
			"                for dy in range(-expand, expand + 1):",
			"                    if max(abs(dx), abs(dy)) != expand:",
			"                        continue",
			"                    b_items = buckets.get((bx + dx, by + dy))",
			"                    if b_items:",
			"                        found_in_ring = True",
			"                        for pid, px, py in b_items:",
			"                            d_sq = (qx - px) ** 2 + (qy - py) ** 2",
			"                            if d_sq < best_dist_sq:",
			"                                best_dist_sq = d_sq",
			"                                best_item = (pid, px, py, d_sq)",
			"",
			"            # 見つかった点までの距離が、次に探索する外枠バケット境界より小さければ終了確定",
			"            if best_item is not None:",
			"                min_dist_to_next_ring = (expand * self.bucket_size) ** 2",
			"                if best_dist_sq <= min_dist_to_next_ring:",
			"                    break",
			"",
			"            expand += 1",
			"            if not found_in_ring and expand > 5:  # バケット過疎時の打ち切り",
			"                break",
			"",
			"        return best_item"
		],
		"description": "AHC 2D Spatial Bucket Grid Index for PyPy"
	},

	"AHC Bitboard Grid Simulator": {
		"prefix": "ahc-bitboard",
		"body": [
			"class BitboardGrid:",
			"    \"\"\"",
			"    Pythonの多倍長整数を利用した、盤面全体を一括判定するビット並列化シミュレータ。",
			"    各行を1つの整数として持ち、上下左右への到達可能性や衝突判定をビット論理演算で処理。",
			"    \"\"\"",
			"    __slots__ = ('h', 'w', 'full_mask', 'rows')",
			"",
			"    def __init__(self, h, w):",
			"        self.h = h",
			"        self.w = w",
			"        self.full_mask = (1 << w) - 1",
			"        self.rows = [0] * h",
			"",
			"    def set_cell(self, r, c):",
			"        self.rows[r] |= (1 << c)",
			"",
			"    def clear_cell(self, r, c):",
			"        self.rows[r] &= ~(1 << c)",
			"",
			"    def get_cell(self, r, c):",
			"        return (self.rows[r] >> c) & 1",
			"",
			"    def step_expand_4dir(self, wall_board):",
			"        \"\"\"現在の配置から4近傍へ1マス進めた到達可能集合を一括計算 (壁との衝突判定込み)\"\"\"",
			"        new_rows = [0] * self.h",
			"        w_mask = self.full_mask",
			"        for r in range(self.h):",
			"            curr = self.rows[r]",
			"            # 左右シフト (盤面内マスクを適用)",
			"            expanded = curr | ((curr << 1) & w_mask) | (curr >> 1)",
			"            # 上下方向からの伝播",
			"            if r > 0:",
			"                expanded |= self.rows[r - 1]",
			"            if r + 1 < self.h:",
			"                expanded |= self.rows[r + 1]",
			"            # 壁セル (wall_board) との AND NOT",
			"            new_rows[r] = expanded & (~wall_board.rows[r]) & w_mask",
			"        return new_rows",
			"",
			"    def count_bits(self):",
			"        \"\"\"全マスの合計立っているビット数\"\"\"",
			"        return sum(bin(r).count('1') for r in self.rows)"
		],
		"description": "AHC Bit-parallel Grid Simulator for PyPy"
	},

	"AHC Weighted Roulette / Softmax Selection": {
		"prefix": "ahc-roulette",
		"body": [
			"import math",
			"",
			"class RouletteSelector:",
			"    \"\"\"",
			"    ランダム貪欲や局所探索の近傍選択用。温度付き確率選択を高速に処理。",
			"    \"\"\"",
			"    __slots__ = ('rng_x',)",
			"",
			"    def __init__(self, seed=88172645463325252):",
			"        self.rng_x = seed",
			"",
			"    def next_double(self):",
			"        self.rng_x ^= (self.rng_x << 13) & 0xFFFFFFFFFFFFFFFF",
			"        self.rng_x ^= (self.rng_x >> 7) & 0xFFFFFFFFFFFFFFFF",
			"        self.rng_x ^= (self.rng_x << 17) & 0xFFFFFFFFFFFFFFFF",
			"        return (self.rng_x >> 11) * (1.0 / (1 << 53))",
			"",
			"    def select(self, candidates, weights):",
			"        \"\"\"正の重みリストに基づいたルーレット選択 O(K)\"\"\"",
			"        total_w = sum(weights)",
			"        if total_w <= 0:",
			"            return candidates[int(self.next_double() * len(candidates))]",
			"        target = self.next_double() * total_w",
			"        acc = 0.0",
			"        for item, w in zip(candidates, weights):",
			"            acc += w",
			"            if acc >= target:",
			"                return item",
			"        return candidates[-1]",
			"",
			"    def select_softmax(self, candidates, scores, temperature=1.0):",
			"        \"\"\"スコアを温度付きソフトマックス確率に変換して選択 (最大化)\"\"\"",
			"        max_s = max(scores)",
			"        inv_temp = 1.0 / temperature",
			"        # オーバーフロー防止のため max_s を引いてから exp",
			"        weights = [math.exp((s - max_s) * inv_temp) for s in scores]",
			"        return self.select(candidates, weights)"
		],
		"description": "AHC Fast Roulette and Softmax Selection for PyPy"
	},

	"AHC Small TSP Bit DP": {
		"prefix": "ahc-tsp-bitdp",
		"body": [
			"import sys",
			"import time",
			"",
			"def solve_sub_tsp(n, dist_matrix, start_node=0, return_to_start=True):",
			"    \"\"\"",
			"    N <= 16 程度の小規模巡回路/パスを厳密最適化 (Bit DP + 復元)。",
			"    焼きなまし・貪欲法中の局所厳密再構成 (LNS) で使用。",
			"    dist_matrix: NxN の距離・コスト行列",
			"    \"\"\"",
			"    perf_counter = time.perf_counter",
			"    start_time = perf_counter()",
			"",
			"    INF = 10**15",
			"    num_states = 1 << n",
			"    # dp[mask][u]: 訪問済み集合 mask で、現在頂点 u にいる最小コスト",
			"    dp = [INF] * (num_states * n)",
			"    parent = [-1] * (num_states * n)",
			"",
			"    start_idx = (1 << start_node) * n + start_node",
			"    dp[start_idx] = 0",
			"",
			"    # 状態遷移: 立っているビット数が少ない集合から順に更新",
			"    for mask in range(num_states):",
			"        mask_offset = mask * n",
			"        for u in range(n):",
			"            curr_cost = dp[mask_offset + u]",
			"            if curr_cost >= INF:",
			"                continue",
			"",
			"            d_u = dist_matrix[u]",
			"            for v in range(n):",
			"                if (mask >> v) & 1 == 0:",
			"                    next_mask = mask | (1 << v)",
			"                    target_idx = next_mask * n + v",
			"                    new_cost = curr_cost + d_u[v]",
			"                    if new_cost < dp[target_idx]:",
			"                        dp[target_idx] = new_cost",
			"                        parent[target_idx] = u",
			"",
			"    # 最良スコアと終端頂点の特定",
			"    full_mask = num_states - 1",
			"    full_offset = full_mask * n",
			"    best_cost = INF",
			"    best_last_node = -1",
			"",
			"    if return_to_start:",
			"        for u in range(n):",
			"            cost = dp[full_offset + u] + dist_matrix[u][start_node]",
			"            if cost < best_cost:",
			"                best_cost = cost",
			"                best_last_node = u",
			"    else:",
			"        for u in range(n):",
			"            cost = dp[full_offset + u]",
			"            if cost < best_cost:",
			"                best_cost = cost",
			"                best_last_node = u",
			"",
			"    # 経路復元",
			"    tour = []",
			"    curr_mask = full_mask",
			"    curr_node = best_last_node",
			"",
			"    while curr_node != -1:",
			"        tour.append(curr_node)",
			"        p = parent[curr_mask * n + curr_node]",
			"        curr_mask &= ~(1 << curr_node)",
			"        curr_node = p",
			"    tour.reverse()",
			"",
			"    if return_to_start:",
			"        tour.append(start_node)",
			"",
			"    # デバッグ出力",
			"    elapsed_ms = (perf_counter() - start_time) * 1000",
			"    print(f'[DEBUG] === Small TSP Bit DP (N={n}) ===', file=sys.stderr)",
			"    print(f'[DEBUG] Best Cost    : {best_cost}', file=sys.stderr)",
			"    print(f'[DEBUG] Tour Length  : {len(tour)}', file=sys.stderr)",
			"    print(f'[DEBUG] Elapsed Time : {elapsed_ms:.2f} ms', file=sys.stderr)",
			"    print(f'[DEBUG] ===============================', file=sys.stderr)",
			"",
			"    return best_cost, tour"
		],
		"description": "AHC Small TSP Bit DP (N<=16) with Path Reconstruction for PyPy"
	},

    "AHC Hungarian Algorithm (O(N^3) Matching)": {
		"prefix": "ahc-hungarian",
		"body": [
			"import sys",
			"import time",
			"",
			"def hungarian(cost_matrix):",
			"    \"\"\"",
			"    最小重み二部マッチング (ハンガリアン法 O(N^3))。",
			"    cost_matrix: N x M の2次元リスト (N <= M を満たすこと。最大化は符号反転して適用)",
			"    戻り値: (min_total_cost, match_row_to_col)",
			"    \"\"\"",
			"    perf_counter = time.perf_counter",
			"    start_time = perf_counter()",
			"",
			"    n = len(cost_matrix)",
			"    m = len(cost_matrix[0])",
			"    assert n <= m, 'Row count must be <= Column count'",
			"",
			"    INF = 10**15",
			"    # 1-indexed で処理",
			"    u = [0] * (n + 1)",
			"    v = [0] * (m + 1)",
			"    p = [0] * (m + 1)  # 各列にマッチした行",
			"    way = [0] * (m + 1)",
			"",
			"    for i in range(1, n + 1):",
			"        p[0] = i",
			"        j0 = 0",
			"        minv = [INF] * (m + 1)",
			"        used = [False] * (m + 1)",
			"",
			"        while True:",
			"            used[j0] = True",
			"            i0 = p[j0]",
			"            delta = INF",
			"            j1 = 0",
			"            row_costs = cost_matrix[i0 - 1]",
			"",
			"            for j in range(1, m + 1):",
			"                if not used[j]:",
			"                    cur = row_costs[j - 1] - u[i0] - v[j]",
			"                    if cur < minv[j]:",
			"                        minv[j] = cur",
			"                        way[j] = j0",
			"                    if minv[j] < delta:",
			"                        delta = minv[j]",
			"                        j1 = j",
			"",
			"            for j in range(m + 1):",
			"                if used[j]:",
			"                    u[p[j]] += delta",
			"                    v[j] -= delta",
			"                else:",
			"                    minv[j] -= delta",
			"",
			"            j0 = j1",
			"            if p[j0] == 0:",
			"                break",
			"",
			"        while True:",
			"            j1 = way[j0]",
			"            p[j0] = p[j1]",
			"            j0 = j1",
			"            if j0 == 0:",
			"                break",
			"",
			"    match = [-1] * n",
			"    for j in range(1, m + 1):",
			"        if p[j] > 0:",
			"            match[p[j] - 1] = j - 1",
			"",
			"    min_cost = -v[0]",
			"",
			"    # デバッグ出力",
			"    elapsed_ms = (perf_counter() - start_time) * 1000",
			"    print(f'[DEBUG] === Hungarian (Matching) ===', file=sys.stderr)",
			"    print(f'[DEBUG] Total Cost   : {min_cost}', file=sys.stderr)",
			"    print(f'[DEBUG] Matrix Size  : {n}x{m}', file=sys.stderr)",
			"    print(f'[DEBUG] Elapsed Time : {elapsed_ms:.2f} ms', file=sys.stderr)",
			"    print(f'[DEBUG] =============================', file=sys.stderr)",
			"",
			"    return min_cost, match"
		],
		"description": "AHC Optimized Hungarian Algorithm for Minimum Weight Bipartite Matching for PyPy"
	},

	"AHC Monte Carlo Tree Search (MCTS)": {
		"prefix": "ahc-mcts",
		"body": [
			"import sys",
			"import time",
			"import math",
			"import random",
			"",
			"def mcts(root_state, get_actions, apply_action, is_terminal, rollout, time_limit=1.95):",
			"    \"\"\"",
			"    root_state: 初期局面",
			"    get_actions: state -> [action, ...]",
			"    apply_action: (state, action) -> next_state",
			"    is_terminal: state -> bool",
			"    rollout: state -> score (終局までの高速シミュレーション評価)",
			"    \"\"\"",
			"    perf_counter = time.perf_counter",
			"    start_time = perf_counter()",
			"    C = 1.41421356  # UCB1 探索パラメータ sqrt(2)",
			"",
			"    # フラットノード管理 (オブジェクト生成回避)",
			"    # ノード要素: [state, parent_id, action_from_parent, visits, total_score, [child_ids], [untried_actions]]",
			"    nodes = [[root_state, -1, None, 0, 0.0, [], get_actions(root_state)]]",
			"    sim_count = 0",
			"",
			"    while True:",
			"        if (sim_count & 127) == 0 and (perf_counter() - start_time >= time_limit):",
			"            break",
			"        sim_count += 1",
			"",
			"        # 1. Selection",
			"        node_id = 0",
			"        while not nodes[node_id][6] and nodes[node_id][5]:  # 未展開アクションがなく子が有る場合",
			"            parent_visits = nodes[node_id][3]",
			"            log_parent = math.log(parent_visits)",
			"            best_ucb = -1e18",
			"            best_child = -1",
			"            for cid in nodes[node_id][5]:",
			"                c_visits = nodes[cid][3]",
			"                c_score = nodes[cid][4]",
			"                ucb = (c_score / c_visits) + C * math.sqrt(log_parent / c_visits)",
			"                if ucb > best_ucb:",
			"                    best_ucb = ucb",
			"                    best_child = cid",
			"            node_id = best_child",
			"",
			"        # 2. Expansion",
			"        if nodes[node_id][6] and not is_terminal(nodes[node_id][0]):",
			"            act = nodes[node_id][6].pop()",
			"            nxt_state = apply_action(nodes[node_id][0], act)",
			"            nxt_node = [nxt_state, node_id, act, 0, 0.0, [], get_actions(nxt_state)]",
			"            nodes.append(nxt_node)",
			"            new_cid = len(nodes) - 1",
			"            nodes[node_id][5].append(new_cid)",
			"            node_id = new_cid",
			"",
			"        # 3. Simulation (Rollout)",
			"        score = rollout(nodes[node_id][0])",
			"",
			"        # 4. Backpropagation",
			"        curr = node_id",
			"        while curr != -1:",
			"            nodes[curr][3] += 1",
			"            nodes[curr][4] += score",
			"            curr = nodes[curr][1]",
			"",
			"    # 訪問回数最多の手を選択",
			"    best_action = None",
			"    max_visits = -1",
			"    for cid in nodes[0][5]:",
			"        if nodes[cid][3] > max_visits:",
			"            max_visits = nodes[cid][3]",
			"            best_action = nodes[cid][2]",
			"",
			"    # デバッグ出力",
			"    elapsed_ms = (perf_counter() - start_time) * 1000",
			"    print(f'[DEBUG] === MCTS ===', file=sys.stderr)",
			"    print(f'[DEBUG] Simulations  : {sim_count}', file=sys.stderr)",
			"    print(f'[DEBUG] Root Visits  : {nodes[0][3]}', file=sys.stderr)",
			"    print(f'[DEBUG] Total Nodes  : {len(nodes)}', file=sys.stderr)",
			"    print(f'[DEBUG] Elapsed Time : {elapsed_ms:.2f} ms', file=sys.stderr)",
			"    print(f'[DEBUG] ============', file=sys.stderr)",
			"",
			"    return best_action"
		],
		"description": "AHC Optimized Monte Carlo Tree Search (MCTS) for PyPy"
	},

	"AHC Coordinate Descent (Continuous Tuning)": {
		"prefix": "ahc-coord-descent",
		"body": [
			"import sys",
			"import time",
			"",
			"def coordinate_descent(params, step_sizes, bounds, evaluate, time_limit=1.95):",
			"    \"\"\"",
			"    連続・離散パラメータのブラックボックス最適化 (最大化)。",
			"    params: 初期パラメータリスト [p0, p1, ...]",
			"    step_sizes: 初期の各次元探索幅 [s0, s1, ...]",
			"    bounds: 各パラメータの探索境界 [(min0, max0), ...]",
			"    evaluate: params -> score",
			"    \"\"\"",
			"    perf_counter = time.perf_counter",
			"    start_time = perf_counter()",
			"",
			"    n = len(params)",
			"    curr_params = params[:]",
			"    curr_steps = step_sizes[:]",
			"    curr_score = evaluate(curr_params)",
			"    best_score = curr_score",
			"    best_params = curr_params[:]",
			"",
			"    iter_count = 0",
			"    improved_any = True",
			"",
			"    while improved_any:",
			"        improved_any = False",
			"        if perf_counter() - start_time >= time_limit:",
			"            break",
			"",
			"        for i in range(n):",
			"            step = curr_steps[i]",
			"            low, high = bounds[i]",
			"            best_step_mult = 0",
			"",
			"            # 正負両方向を試行",
			"            for mult in (1.0, -1.0):",
			"                val = curr_params[i] + mult * step",
			"                if low <= val <= high:",
			"                    curr_params[i] = val",
			"                    score = evaluate(curr_params)",
			"                    iter_count += 1",
			"                    if score > curr_score:",
			"                        curr_score = score",
			"                        best_step_mult = mult",
			"                    curr_params[i] = curr_params[i] - mult * step",
			"",
			"            if best_step_mult != 0:",
			"                curr_params[i] += best_step_mult * step",
			"                improved_any = True",
			"                if curr_score > best_score:",
			"                    best_score = curr_score",
			"                    best_params = curr_params[:]",
			"            else:",
			"                # 改善が見られない次元はステップ幅を半減 (減衰)",
			"                curr_steps[i] *= 0.5",
			"",
			"            if perf_counter() - start_time >= time_limit:",
			"                break",
			"",
			"    # デバッグ出力",
			"    elapsed_ms = (perf_counter() - start_time) * 1000",
			"    print(f'[DEBUG] === Coordinate Descent ===', file=sys.stderr)",
			"    print(f'[DEBUG] Iterations   : {iter_count}', file=sys.stderr)",
			"    print(f'[DEBUG] Best Score   : {best_score}', file=sys.stderr)",
			"    print(f'[DEBUG] Final Params : {best_params}', file=sys.stderr)",
			"    print(f'[DEBUG] Elapsed Time : {elapsed_ms:.2f} ms', file=sys.stderr)",
			"    print(f'[DEBUG] ==========================', file=sys.stderr)",
			"",
			"    return best_params, best_score"
		],
		"description": "AHC Coordinate Descent Parameter Optimizer for PyPy"
	},

	"AHC Priority Topological Sort": {
		"prefix": "ahc-topo-sort",
		"body": [
			"import sys",
			"import heapq",
			"",
			"def priority_topological_sort(num_nodes, edges, get_priority):",
			"    \"\"\"",
			"    依存関係制約を満たす順序付け (優先度付きトポロジカルソート)。",
			"    num_nodes: 頂点数",
			"    edges: [(u, v), ...] u -> v の先行制約",
			"    get_priority: u -> 優先度値 (大きいものから優先展開)",
			"    戻り値: (order, is_dag)",
			"    \"\"\"",
			"    in_deg = [0] * num_nodes",
			"    adj = [[] for _ in range(num_nodes)]",
			"    for u, v in edges:",
			"        adj[u].append(v)",
			"        in_deg[v] += 1",
			"",
			"    # 優先度付きキュー: (-priority, u)",
			"    pq = [(-get_priority(i), i) for i in range(num_nodes) if in_deg[i] == 0]",
			"    heapq.heapify(pq)",
			"    heappush = heapq.heappush",
			"    heappop = heapq.heappop",
			"",
			"    order = []",
			"    while pq:",
			"        _, u = heappop(pq)",
			"        order.append(u)",
			"        for v in adj[u]:",
			"            in_deg[v] -= 1",
			"            if in_deg[v] == 0:",
			"                heappush(pq, (-get_priority(v), v))",
			"",
			"    is_dag = len(order) == num_nodes",
			"    if not is_dag:",
			"        print(f'[WARN] Cycle detected! DAG constraint violated.', file=sys.stderr)",
			"",
			"    return order, is_dag"
		],
		"description": "AHC Priority Topological Sort with Cycle Detection for PyPy"
	},

	"AHC Interactive Loop Framework": {
		"prefix": "ahc-interactive",
		"body": [
			"import sys",
			"import time",
			"",
			"def solve_interactive():",
			"    perf_counter = time.perf_counter",
			"    start_time = perf_counter()",
			"    TOTAL_TIME_LIMIT = ${1:1.95}",
			"",
			"    # 高速入出力バインド",
			"    input_line = sys.stdin.readline",
			"    out_write = sys.stdout.write",
			"    out_flush = sys.stdout.flush",
			"",
			"    # --- 1. 初期入力受領 ---",
			"    # line = input_line()",
			"    # if not line: return",
			"    # N, M = map(int, line.split())",
			"",
			"    MAX_TURNS = ${2:100}",
			"    total_response_time = 0.0",
			"",
			"    for turn in range(MAX_TURNS):",
			"        # ターンごとの時間配分管理",
			"        turn_start = perf_counter()",
			"        elapsed = turn_start - start_time",
			"        if elapsed >= TOTAL_TIME_LIMIT:",
			"            # 時間切迫時の緊急フォールバック行動",
			"            pass",
			"",
			"        # --- 2. 行動の決定 ---",
			"        # act = decide_action(turn)",
			"",
			"        # --- 3. クエリ出力 (改行とflush必須) ---",
			"        # out_write(f'{act}\\\\n')",
			"        # out_flush()",
			"",
			"        # --- 4. ジャッジからの観測結果・フィードバック受領 ---",
			"        # res = input_line().strip()",
			"        # if not res or res == '-1': # 終了またはエラー",
			"        #     break",
			"",
			"        # --- 5. 内部信念・状態の更新 (ベイズ更新 / 盤面同期) ---",
			"        # update_state(act, res)",
			"",
			"        total_response_time += (perf_counter() - turn_start)",
			"",
			"    # デバッグ出力",
			"    total_time_ms = (perf_counter() - start_time) * 1000",
			"    avg_turn_ms = (total_response_time / max(1, MAX_TURNS)) * 1000",
			"    print(f'[DEBUG] === Interactive Loop Finished ===', file=sys.stderr)",
			"    print(f'[DEBUG] Total Elapsed : {total_time_ms:.2f} ms', file=sys.stderr)",
			"    print(f'[DEBUG] Avg Turn Time : {avg_turn_ms:.3f} ms', file=sys.stderr)",
			"    print(f'[DEBUG] ==================================', file=sys.stderr)"
		],
		"description": "AHC Interactive/Reactive Query Loop Template for PyPy"
	},

    "AHC k-means (2D Clustering)": {
		"prefix": "ahc-kmeans",
		"body": [
			"import sys",
			"import time",
			"import random",
			"",
			"def kmeans_2d(points, k, max_iter=30):",
			"    \"\"\"",
			"    2次元座標群を k 個のクラスタに分割 (分割統治・小問題化用)。",
			"    points: [(x, y), ...]",
			"    戻り値: (centroids, cluster_assignments)",
			"    \"\"\"",
			"    perf_counter = time.perf_counter",
			"    start_time = perf_counter()",
			"    n = len(points)",
			"    assert n >= k",
			"",
			"    # 初期重心の選択 (k-means++)",
			"    centroids = [points[random.randrange(n)]]",
			"    for _ in range(1, k):",
			"        dist_sqs = []",
			"        for px, py in points:",
			"            min_d2 = min((px - cx) ** 2 + (py - cy) ** 2 for cx, cy in centroids)",
			"            dist_sqs.append(min_d2)",
			"        total_d2 = sum(dist_sqs)",
			"        r = random.uniform(0, total_d2)",
			"        acc = 0.0",
			"        for idx, d2 in enumerate(dist_sqs):",
			"            acc += d2",
			"            if acc >= r:",
			"                centroids.append(points[idx])",
			"                break",
			"",
			"    assignments = [0] * n",
			"    for it in range(max_iter):",
			"        changed = False",
			"        # 1. 各点を最寄りの重心に割り当て",
			"        for i in range(n):",
			"            px, py = points[i]",
			"            best_d2 = 10**18",
			"            best_c = 0",
			"            for c in range(k):",
			"                cx, cy = centroids[c]",
			"                d2 = (px - cx) ** 2 + (py - cy) ** 2",
			"                if d2 < best_d2:",
			"                    best_d2 = d2",
			"                    best_c = c",
			"            if assignments[i] != best_c:",
			"                assignments[i] = best_c",
			"                changed = True",
			"",
			"        if not changed:",
			"            break",
			"",
			"        # 2. 重心の再計算",
			"        sum_x = [0.0] * k",
			"        sum_y = [0.0] * k",
			"        counts = [0] * k",
			"        for i in range(n):",
			"            c = assignments[i]",
			"            px, py = points[i]",
			"            sum_x[c] += px",
			"            sum_y[c] += py",
			"            counts[c] += 1",
			"",
			"        for c in range(k):",
			"            if counts[c] > 0:",
			"                centroids[c] = (sum_x[c] / counts[c], sum_y[c] / counts[c])",
			"",
			"    # デバッグ出力",
			"    elapsed_ms = (perf_counter() - start_time) * 1000",
			"    print(f'[DEBUG] === k-means (k={k}) ===', file=sys.stderr)",
			"    print(f'[DEBUG] Points Count : {n}', file=sys.stderr)",
			"    print(f'[DEBUG] Iterations   : {it + 1}/{max_iter}', file=sys.stderr)",
			"    print(f'[DEBUG] Elapsed Time : {elapsed_ms:.2f} ms', file=sys.stderr)",
			"    print(f'[DEBUG] ========================', file=sys.stderr)",
			"",
			"    return centroids, assignments"
		],
		"description": "AHC 2D k-means Clustering with k-means++ initialization for PyPy"
	},

	"AHC Convex Hull (Monotone Chain)": {
		"prefix": "ahc-convexhull",
		"body": [
			"import sys",
			"import time",
			"",
			"def convex_hull_2d(points):",
			"    \"\"\"",
			"    2次元点群の外殻を反時計回りに抽出 (Monotone Chain 法 O(N log N))。",
			"    points: [(x, y), ...]",
			"    戻り値: 凸包をなす点のリスト (反時計回り)",
			"    \"\"\"",
			"    perf_counter = time.perf_counter",
			"    start_time = perf_counter()",
			"    n = len(points)",
			"    if n <= 2:",
			"        return points[:]",
			"",
			"    # x座標昇順 (同値ならy座標昇順)",
			"    pts = sorted(points)",
			"",
			"    def cross(o, a, b):",
			"        return (a[0] - o[0]) * (b[1] - o[1]) - (a[1] - o[1]) * (b[0] - o[0])",
			"",
			"    # 下側凸包",
			"    lower = []",
			"    for p in pts:",
			"        while len(lower) >= 2 and cross(lower[-2], lower[-1], p) <= 0:",
			"            lower.pop()",
			"        lower.append(p)",
			"",
			"    # 上側凸包",
			"    upper = []",
			"    for p in reversed(pts):",
			"        while len(upper) >= 2 and cross(upper[-2], upper[-1], p) <= 0:",
			"            upper.pop()",
			"        upper.append(p)",
			"",
			"    # 各リストの末尾（相手の始点）を除去して結合",
			"    hull = lower[:-1] + upper[:-1]",
			"",
			"    # デバッグ出力",
			"    elapsed_ms = (perf_counter() - start_time) * 1000",
			"    print(f'[DEBUG] === Convex Hull ===', file=sys.stderr)",
			"    print(f'[DEBUG] Input Points : {n}', file=sys.stderr)",
			"    print(f'[DEBUG] Hull Vertices: {len(hull)}', file=sys.stderr)",
			"    print(f'[DEBUG] Elapsed Time : {elapsed_ms:.2f} ms', file=sys.stderr)",
			"    print(f'[DEBUG] ===================', file=sys.stderr)",
			"",
			"    return hull"
		],
		"description": "AHC 2D Convex Hull (Monotone Chain) for PyPy"
	},

	"AHC Large Neighborhood Search (LNS)": {
		"prefix": "ahc-lns",
		"body": [
			"import sys",
			"import time",
			"import random",
			"",
			"def large_neighborhood_search():",
			"    \"\"\"",
			"    局所最適解（プラトー）を脱出するための大規模近傍探索 (Ruin and Recreate)。",
			"    \"\"\"",
			"    perf_counter = time.perf_counter",
			"    start_time = perf_counter()",
			"    TIME_LIMIT = ${1:1.95}",
			"    RUIN_RATE = ${2:0.25}  # 破壊する割合 (20%〜30%)",
			"",
			"    # TODO: 初期解の生成と評価",
			"    # current_state = generate_initial_state()",
			"    # current_score = evaluate(current_state)",
			"    # best_state = current_state[:]",
			"    # best_score = current_score",
			"",
			"    iter_count = 0",
			"    accepted_count = 0",
			"    improved_count = 0",
			"",
			"    while True:",
			"        if (iter_count & 31) == 0 and (perf_counter() - start_time >= TIME_LIMIT):",
			"            break",
			"        iter_count += 1",
			"",
			"        # --- 1. Ruin (一部を意図的に破壊・除去) ---",
			"        # partial_state, removed_info = ruin(current_state, RUIN_RATE)",
			"",
			"        # --- 2. Recreate (貪欲法・DP等で空いた箇所を最適再構築) ---",
			"        # cand_state = recreate(partial_state, removed_info)",
			"        # cand_score = evaluate(cand_state)",
			"",
			"        # --- 3. 採択判定 (最大化) ---",
			"        # diff = cand_score - current_score",
			"        # if diff >= 0:  # または一定確率で改悪受容",
			"        #     current_state = cand_state",
			"        #     current_score = cand_score",
			"        #     accepted_count += 1",
			"        #     if current_score > best_score:",
			"        #         best_score = current_score",
			"        #         best_state = cand_state[:]",
			"        #         improved_count += 1",
			"        pass",
			"",
			"    # デバッグ出力",
			"    total_time_ms = (perf_counter() - start_time) * 1000",
			"    print(f'[DEBUG] === Large Neighborhood Search (LNS) ===', file=sys.stderr)",
			"    print(f'[DEBUG] Iterations   : {iter_count}', file=sys.stderr)",
			"    print(f'[DEBUG] Accepted     : {accepted_count}', file=sys.stderr)",
			"    print(f'[DEBUG] Improved     : {improved_count}', file=sys.stderr)",
			"    print(f'[DEBUG] Best Score   : {best_score}', file=sys.stderr)",
			"    print(f'[DEBUG] Elapsed Time : {total_time_ms:.2f} ms', file=sys.stderr)",
			"    print(f'[DEBUG] ======================================', file=sys.stderr)",
			"",
			"    # return best_state"
		],
		"description": "AHC Large Neighborhood Search (Ruin and Recreate) Template for PyPy"
	},

	"AHC Genetic Algorithm (Order / Uniform Crossover)": {
		"prefix": "ahc-ga",
		"body": [
			"import sys",
			"import time",
			"import random",
			"",
			"def genetic_algorithm():",
			"    perf_counter = time.perf_counter",
			"    start_time = perf_counter()",
			"    TIME_LIMIT = ${1:1.95}",
			"    POP_SIZE = ${2:50}      # 個体数",
			"    ELITE_COUNT = ${3:4}    # エリート保存個体数",
			"    MUTATION_RATE = ${4:0.1} # 突然変異率",
			"",
			"    # --- 順序交叉 (OX: Order Crossover - 巡回・順列用) ---",
			"    def crossover_ox(p1, p2):",
			"        n = len(p1)",
			"        l = random.randrange(n)",
			"        r = random.randrange(n)",
			"        if l > r:",
			"            l, r = r, l",
			"        child = [-1] * n",
			"        child[l:r+1] = p1[l:r+1]",
			"        used = set(child[l:r+1])",
			"        idx = (r + 1) % n",
			"        for val in p2[r+1:] + p2[:r+1]:",
			"            if val not in used:",
			"                child[idx] = val",
			"                idx = (idx + 1) % n",
			"        return child",
			"",
			"    # TODO: 初期個体群の生成 (評価値, 個体)",
			"    # population = []",
			"    # for _ in range(POP_SIZE):",
			"    #     ind = generate_individual()",
			"    #     population.append((evaluate(ind), ind))",
			"",
			"    generation = 0",
			"",
			"    while True:",
			"        if (generation & 15) == 0 and (perf_counter() - start_time >= TIME_LIMIT):",
			"            break",
			"        generation += 1",
			"",
			"        # スコア降順ソート (最大化)",
			"        # population.sort(key=lambda x: x[0], reverse=True)",
			"        # next_pop = population[:ELITE_COUNT]  # エリート保存",
			"",
			"        # 次世代の生成 (トーナメント選択 + 交叉 + 突然変異)",
			"        # while len(next_pop) < POP_SIZE:",
			"        #     # トーナメントサイズ 3",
			"        #     cand1 = random.sample(population, 3)",
			"        #     p1 = max(cand1, key=lambda x: x[0])[1]",
			"        #     cand2 = random.sample(population, 3)",
			"        #     p2 = max(cand2, key=lambda x: x[0])[1]",
			"        #     child = crossover_ox(p1, p2)",
			"        #     if random.random() < MUTATION_RATE:",
			"        #         mutate(child)",
			"        #     next_pop.append((evaluate(child), child))",
			"        # population = next_pop",
			"        pass",
			"",
			"    # デバッグ出力",
			"    total_time_ms = (perf_counter() - start_time) * 1000",
			"    print(f'[DEBUG] === Genetic Algorithm ===', file=sys.stderr)",
			"    print(f'[DEBUG] Generations  : {generation}', file=sys.stderr)",
			"    # print(f'[DEBUG] Best Score   : {population[0][0]}', file=sys.stderr)",
			"    print(f'[DEBUG] Elapsed Time : {total_time_ms:.2f} ms', file=sys.stderr)",
			"    print(f'[DEBUG] =========================', file=sys.stderr)",
			"",
			"    # return population[0][1]"
		],
		"description": "AHC Lightweight Genetic Algorithm for PyPy"
	},

	"AHC Dinic (Max Flow / Min Cut)": {
		"prefix": "ahc-dinic",
		"body": [
			"import sys",
			"import time",
			"from collections import deque",
			"",
			"class Dinic:",
			"    \"\"\"",
			"    Dinic法による最大流および最小カット (領域二分割等に使用)。",
			"    PyPy向けにフラット配列構造で最適化。",
			"    \"\"\"",
			"    __slots__ = ('n', 'graph', 'level', 'it')",
			"",
			"    def __init__(self, n):",
			"        self.n = n",
			"        self.graph = [[] for _ in range(n)]",
			"        self.level = [-1] * n",
			"        self.it = [0] * n",
			"",
			"    def add_edge(self, from_u, to_v, cap):",
			"        forward = [to_v, cap, len(self.graph[to_v])]",
			"        backward = [from_u, 0, len(self.graph[from_u])]",
			"        self.graph[from_u].append(forward)",
			"        self.graph[to_v].append(backward)",
			"",
			"    def _bfs(self, s, t):",
			"        self.level = [-1] * self.n",
			"        self.level[s] = 0",
			"        q = deque([s])",
			"        graph = self.graph",
			"        level = self.level",
			"        while q:",
			"            u = q.popleft()",
			"            d = level[u] + 1",
			"            for v, cap, _ in graph[u]:",
			"                if cap > 0 and level[v] == -1:",
			"                    level[v] = d",
			"                    q.append(v)",
			"        return level[t] != -1",
			"",
			"    def _dfs(self, u, t, flow):",
			"        if u == t:",
			"            return flow",
			"        graph = self.graph",
			"        level = self.level",
			"        it = self.it",
			"        for i in range(it[u], len(graph[u])):",
			"            it[u] = i",
			"            v, cap, rev_idx = graph[u][i]",
			"            if cap > 0 and level[u] < level[v]:",
			"                f = flow if flow < cap else cap",
			"                pushed = self._dfs(v, t, f)",
			"                if pushed > 0:",
			"                    graph[u][i][1] -= pushed",
			"                    graph[v][rev_idx][1] += pushed",
			"                    return pushed",
			"        return 0",
			"",
			"    def max_flow(self, s, t):",
			"        flow = 0",
			"        INF = 10**18",
			"        while self._bfs(s, t):",
			"            self.it = [0] * self.n",
			"            while True:",
			"                f = self._dfs(s, t, INF)",
			"                if f == 0:",
			"                    break",
			"                flow += f",
			"        return flow",
			"",
			"    def min_cut(self, s):",
			"        \"\"\"残余グラフ上で s から到達可能な頂点集合 (True: s側, False: t側)\"\"\"",
			"        visited = [False] * self.n",
			"        visited[s] = True",
			"        q = deque([s])",
			"        while q:",
			"            u = q.popleft()",
			"            for v, cap, _ in self.graph[u]:",
			"                if cap > 0 and not visited[v]:",
			"                    visited[v] = True",
			"                    q.append(v)",
			"        return visited"
		],
		"description": "AHC Dinic Algorithm for Max-Flow / Min-Cut for PyPy"
	},

	"AHC LCA (Lowest Common Ancestor / Doubling)": {
		"prefix": "ahc-lca",
		"body": [
			"import sys",
			"import time",
			"",
			"class TreeLCA:",
			"    \"\"\"",
			"    ダブリングによる木構造の最近共通祖先 (LCA) および2頂点間パス長計算 O(log N)。",
			"    \"\"\"",
			"    __slots__ = ('n', 'log_n', 'depth', 'up')",
			"",
			"    def __init__(self, n, adj, root=0):",
			"        self.n = n",
			"        # 必要ビット数 (2^k >= n)",
			"        log_n = (n - 1).bit_length() if n > 1 else 1",
			"        self.log_n = log_n",
			"        depth = [-1] * n",
			"        # up[k * n + u] で (1<<k) 個上の親ノードをフラット保持",
			"        up = [-1] * (log_n * n)",
			"",
			"        # BFS で深さと親ノード (k=0) を初期化",
			"        stack = [root]",
			"        depth[root] = 0",
			"        while stack:",
			"            u = stack.pop()",
			"            d = depth[u] + 1",
			"            for v in adj[u]:",
			"                if depth[v] == -1:",
			"                    depth[v] = d",
			"                    up[v] = u",
			"                    stack.append(v)",
			"",
			"        # ダブリングテーブル構築",
			"        for k in range(1, log_n):",
			"            curr_offset = k * n",
			"            prev_offset = (k - 1) * n",
			"            for u in range(n):",
			"                p = up[prev_offset + u]",
			"                up[curr_offset + u] = up[prev_offset + p] if p != -1 else -1",
			"",
			"        self.depth = depth",
			"        self.up = up",
			"",
			"    def get_lca(self, u, v):",
			"        depth = self.depth",
			"        up = self.up",
			"        n = self.n",
			"        log_n = self.log_n",
			"",
			"        if depth[u] < depth[v]:",
			"            u, v = v, u",
			"",
			"        # 同じ深さまで持ち上げる",
			"        diff = depth[u] - depth[v]",
			"        for k in range(log_n):",
			"            if (diff >> k) & 1:",
			"                u = up[k * n + u]",
			"",
			"        if u == v:",
			"            return u",
			"",
			"        # 共通の親の直前まで2冪ジャンプ",
			"        for k in range(log_n - 1, -1, -1):",
			"            offset = k * n",
			"            pu = up[offset + u]",
			"            pv = up[offset + v]",
			"            if pu != pv:",
			"                u = pu",
			"                v = pv",
			"",
			"        return up[u]",
			"",
			"    def get_dist(self, u, v):",
			"        lca = self.get_lca(u, v)",
			"        return self.depth[u] + self.depth[v] - 2 * self.depth[lca]"
		],
		"description": "AHC Doubling LCA (Lowest Common Ancestor) for PyPy"
	},

    "AHC Adaptive Large Neighborhood Search (ALNS)": {
		"prefix": "ahc-alns",
		"body": [
			"import sys",
			"import time",
			"import random",
			"",
			"def alns():",
			"    perf_counter = time.perf_counter",
			"    start_time = perf_counter()",
			"    TIME_LIMIT = ${1:1.95}",
			"",
			"    # 近傍操作のリスト (関数やメソッドを登録)",
			"    # 各近傍は (state) -> candidate_state を返す",
			"    # destroy_operators = [ruin_random, ruin_cluster, ruin_worst]",
			"    # repair_operators = [recreate_greedy, recreate_regret]",
			"    num_ops = ${2:4}  # 近傍の総数",
			"    weights = [1.0] * num_ops",
			"    scores_record = [0.0] * num_ops",
			"    counts = [0] * num_ops",
			"",
			"    # 報酬パラメータ",
			"    R_BEST = 5.0      # グローバル最良更新",
			"    R_BETTER = 2.0    # 現状態より改善",
			"    R_ACCEPT = 1.0    # 採択 (SA等で悪化受容時)",
			"    DECAY = 0.8       # 重みの減衰率 (過去の累積影響度)",
			"",
			"    # TODO: 初期状態の構築",
			"    # current_state = ...",
			"    # current_score = evaluate(current_state)",
			"    # best_score = current_score",
			"    # best_state = current_state[:]",
			"",
			"    iter_count = 0",
			"    SEGMENT_SIZE = 100  # 重み更新の周期",
			"",
			"    while True:",
			"        if (iter_count & 63) == 0 and (perf_counter() - start_time >= TIME_LIMIT):",
			"            break",
			"        iter_count += 1",
			"",
			"        # 1. ルーレット選択で近傍を決定",
			"        total_w = sum(weights)",
			"        r = random.uniform(0, total_w)",
			"        acc = 0.0",
			"        op_idx = 0",
			"        for idx, w in enumerate(weights):",
			"            acc += w",
			"            if acc >= r:",
			"                op_idx = idx",
			"                break",
			"        counts[op_idx] += 1",
			"",
			"        # 2. 近傍の適用と評価",
			"        # cand_state = apply_operator(op_idx, current_state)",
			"        # cand_score = evaluate(cand_state)",
			"        # diff = cand_score - current_score",
			"",
			"        # 3. 採択判定と報酬付与 (最大化の例)",
			"        reward = 0.0",
			"        # if cand_score > best_score:",
			"        #     best_score = cand_score",
			"        #     best_state = cand_state[:]",
			"        #     current_state = cand_state",
			"        #     current_score = cand_score",
			"        #     reward = R_BEST",
			"        # elif diff > 0:",
			"        #     current_state = cand_state",
			"        #     current_score = cand_score",
			"        #     reward = R_BETTER",
			"        # elif accept_sa(diff):",
			"        #     current_state = cand_state",
			"        #     current_score = cand_score",
			"        #     reward = R_ACCEPT",
			"        scores_record[op_idx] += reward",
			"",
			"        # 4. 定期的な重み更新",
			"        if iter_count % SEGMENT_SIZE == 0:",
			"            for i in range(num_ops):",
			"                if counts[i] > 0:",
			"                    p = scores_record[i] / counts[i]",
			"                    weights[i] = weights[i] * DECAY + p * (1.0 - DECAY)",
			"                weights[i] = max(0.05, weights[i])  # 最小選択確率を担保",
			"                scores_record[i] = 0.0",
			"                counts[i] = 0",
			"",
			"    # デバッグ出力",
			"    total_time_ms = (perf_counter() - start_time) * 1000",
			"    print(f'[DEBUG] === ALNS Finished ===', file=sys.stderr)",
			"    print(f'[DEBUG] Iterations   : {iter_count}', file=sys.stderr)",
			"    print(f'[DEBUG] Final Weights: {[round(w, 3) for w in weights]}', file=sys.stderr)",
			"    # print(f'[DEBUG] Best Score   : {best_score}', file=sys.stderr)",
			"    print(f'[DEBUG] Elapsed Time : {total_time_ms:.2f} ms', file=sys.stderr)",
			"    print(f'[DEBUG] =====================', file=sys.stderr)",
			"",
			"    # return best_state"
		],
		"description": "AHC Adaptive Large Neighborhood Search (ALNS) for PyPy"
	},

	"AHC 2D Geometry Basics (Intersection & Inside Polygon)": {
		"prefix": "ahc-geometry-2d",
		"body": [
			"class Geometry2D:",
			"    \"\"\"",
			"    連続平面上の線分交差判定およびポリゴン内外判定。",
			"    \"\"\"",
			"    @staticmethod",
			"    def ccw(ax, ay, bx, by, cx, cy):",
			"        \"\"\"外積による3点の位置関係 (1: 反時計回り, -1: 時計回り, 0: 一直線上)\"\"\"",
			"        cross = (bx - ax) * (cy - ay) - (by - ay) * (cx - ax)",
			"        if cross > 1e-9:",
			"            return 1",
			"        if cross < -1e-9:",
			"            return -1",
			"        return 0",
			"",
			"    @classmethod",
			"    def intersect_segments(cls, p1, p2, p3, p4):",
			"        \"\"\"線分 p1-p2 と p3-p4 の交差判定 (端点共有・接触を含む)\"\"\"",
			"        x1, y1 = p1",
			"        x2, y2 = p2",
			"        x3, y3 = p3",
			"        x4, y4 = p4",
			"        ccw = cls.ccw",
			"",
			"        # バウンディングボックスの重なり判定 (高速除外)",
			"        if max(x1, x2) < min(x3, x4) or max(x3, x4) < min(x1, x2) or \\",
			"           max(y1, y2) < min(y3, y4) or max(y3, y4) < min(y1, y2):",
			"            return False",
			"",
			"        c1 = ccw(x1, y1, x2, y2, x3, y3)",
			"        c2 = ccw(x1, y1, x2, y2, x4, y4)",
			"        c3 = ccw(x3, y3, x4, y4, x1, y1)",
			"        c4 = ccw(x3, y3, x4, y4, x2, y2)",
			"        return (c1 * c2 <= 0) and (c3 * c4 <= 0)",
			"",
			"    @staticmethod",
			"    def is_inside_polygon(qx, qy, polygon):",
			"        \"\"\"",
			"        点 (qx, qy) が多角形 polygon: [(x, y), ...] 内にあるかを判定 (Ray Casting法)。",
			"        戻り値: 1 (内部), 0 (外部), -1 (辺上)",
			"        \"\"\"",
			"        n = len(polygon)",
			"        inside = False",
			"        for i in range(n):",
			"            x1, y1 = polygon[i]",
			"            x2, y2 = polygon[(i + 1) % n]",
			"",
			"            # 辺上にあるかの厳密判定",
			"            cross = (x2 - x1) * (qy - y1) - (y2 - y1) * (qx - x1)",
			"            if abs(cross) < 1e-9 and min(x1, x2) <= qx <= max(x1, x2) and min(y1, y2) <= qy <= max(y1, y2):",
			"                return -1  # 境界上",
			"",
			"            # 水平半直線との交差回数",
			"            if ((y1 > qy) != (y2 > qy)) and \\",
			"               (qx < (x2 - x1) * (qy - y1) / (y2 - y1) + x1):",
			"                inside = not inside",
			"        return 1 if inside else 0"
		],
		"description": "AHC Fast 2D Geometry (Line Intersection & Point-in-Polygon) for PyPy"
	},

	"AHC Min-Cost Flow (Primal-Dual)": {
		"prefix": "ahc-min-cost-flow",
		"body": [
			"import sys",
			"import heapq",
			"",
			"class MinCostFlow:",
			"    \"\"\"",
			"    Primal-Dual法による最小費用流 (負辺なし、または初期ポテンシャル0で適用)。",
			"    PyPy向けにフラット配列・ヒープで最適化。",
			"    \"\"\"",
			"    __slots__ = ('n', 'graph')",
			"",
			"    def __init__(self, n):",
			"        self.n = n",
			"        self.graph = [[] for _ in range(n)]",
			"",
			"    def add_edge(self, from_u, to_v, cap, cost):",
			"        forward = [to_v, cap, cost, len(self.graph[to_v])]",
			"        backward = [from_u, 0, -cost, len(self.graph[from_u])]",
			"        self.graph[from_u].append(forward)",
			"        self.graph[to_v].append(backward)",
			"",
			"    def flow(self, s, t, max_f):",
			"        \"\"\"始点 s から終点 t へ最大流量 max_f を流す最小費用を計算 (total_flow, total_cost)\"\"\"",
			"        n = self.n",
			"        graph = self.graph",
			"        res_flow = 0",
			"        res_cost = 0",
			"        potential = [0] * n",
			"        INF = 10**18",
			"",
			"        heappush = heapq.heappush",
			"        heappop = heapq.heappop",
			"",
			"        while res_flow < max_f:",
			"            dist = [INF] * n",
			"            pv = [-1] * n",
			"            pe = [-1] * n",
			"            dist[s] = 0",
			"            pq = [(0, s)]",
			"",
			"            while pq:",
			"                d, u = heappop(pq)",
			"                if dist[u] < d:",
			"                    continue",
			"                pot_u = potential[u]",
			"                for idx, (v, cap, cost, rev) in enumerate(graph[u]):",
			"                    if cap > 0:",
			"                        new_d = d + cost + pot_u - potential[v]",
			"                        if dist[v] > new_d:",
			"                            dist[v] = new_d",
			"                            pv[v] = u",
			"                            pe[v] = idx",
			"                            heappush(pq, (new_d, v))",
			"",
			"            if dist[t] == INF:",
			"                break  # これ以上流せない",
			"",
			"            for u in range(n):",
			"                if dist[u] < INF:",
			"                    potential[u] += dist[u]",
			"",
			"            # パス上の追加可能流量を計算",
			"            add_f = max_f - res_flow",
			"            curr = t",
			"            while curr != s:",
			"                u = pv[curr]",
			"                idx = pe[curr]",
			"                cap = graph[u][idx][1]",
			"                if cap < add_f:",
			"                    add_f = cap",
			"                curr = u",
			"",
			"            res_flow += add_f",
			"            res_cost += add_f * potential[t]",
			"            curr = t",
			"            while curr != s:",
			"                u = pv[curr]",
			"                idx = pe[curr]",
			"                rev = graph[u][idx][3]",
			"                graph[u][idx][1] -= add_f",
			"                graph[curr][rev][1] += add_f",
			"                curr = u",
			"",
			"        return res_flow, res_cost"
		],
		"description": "AHC Min-Cost Flow (Primal-Dual with Dijkstra) for PyPy"
	},

	"AHC Bin Packing (FFD / BFD)": {
		"prefix": "ahc-binpacking",
		"body": [
			"def bin_packing_ffd(items, capacity):",
			"    \"\"\"",
			"    First Fit Decreasing (FFD) によるビンパッキング近似解法。",
			"    items: 各アイテムのサイズリスト [s0, s1, ...]",
			"    capacity: 各ビンの容量上限",
			"    戻り値: bins (各ビンに含まれるアイテムのインデックスのリスト)",
			"    \"\"\"",
			"    # (サイズ降順, 元のインデックス)",
			"    indexed_items = sorted([(s, i) for i, s in enumerate(items)], reverse=True)",
			"    bin_remains = []",
			"    bins = []",
			"",
			"    for size, orig_idx in indexed_items:",
			"        placed = False",
			"        for b_idx in range(len(bin_remains)):",
			"            if bin_remains[b_idx] >= size:",
			"                bin_remains[b_idx] -= size",
			"                bins[b_idx].append(orig_idx)",
			"                placed = True",
			"                break",
			"        if not placed:",
			"            bin_remains.append(capacity - size)",
			"            bins.append([orig_idx])",
			"",
			"    return bins",
			"",
			"def bin_packing_bfd(items, capacity):",
			"    \"\"\"",
			"    Best Fit Decreasing (BFD) - 空き容量が最も小さくなるビンを優先選択。",
			"    \"\"\"",
			"    indexed_items = sorted([(s, i) for i, s in enumerate(items)], reverse=True)",
			"    bin_remains = []",
			"    bins = []",
			"",
			"    for size, orig_idx in indexed_items:",
			"        best_b = -1",
			"        min_slack = capacity + 1",
			"        for b_idx, rem in enumerate(bin_remains):",
			"            slack = rem - size",
			"            if 0 <= slack < min_slack:",
			"                min_slack = slack",
			"                best_b = b_idx",
			"        if best_b != -1:",
			"            bin_remains[best_b] -= size",
			"            bins[best_b].append(orig_idx)",
			"        else:",
			"            bin_remains.append(capacity - size)",
			"            bins.append([orig_idx])",
			"",
			"    return bins"
		],
		"description": "AHC Fast Bin Packing Heuristics (FFD and BFD) for PyPy"
	},

	"AHC Particle Filter (State Estimation)": {
		"prefix": "ahc-particle-filter",
		"body": [
			"import random",
			"import math",
			"",
			"class ParticleFilter:",
			"    \"\"\"",
			"    不確実性・観測ノイズ下での状態推定 (インタラクティブ問題向け)。",
			"    \"\"\"",
			"    __slots__ = ('num_particles', 'particles', 'weights')",
			"",
			"    def __init__(self, num_particles, init_state_fn):",
			"        self.num_particles = num_particles",
			"        # 各パーティクルは状態表現 (リストやタプル)",
			"        self.particles = [init_state_fn() for _ in range(num_particles)]",
			"        self.weights = [1.0 / num_particles] * num_particles",
			"",
			"    def predict(self, transition_fn):",
			"        \"\"\"制御入力とノイズによる状態遷移\"\"\"",
			"        self.particles = [transition_fn(p) for p in self.particles]",
			"",
			"    def update(self, likelihood_fn):",
			"        \"\"\"観測値に基づく尤度計算と重み更新\"\"\"",
			"        weights = self.weights",
			"        for i, p in enumerate(self.particles):",
			"            weights[i] *= likelihood_fn(p)",
			"",
			"        total_w = sum(weights)",
			"        if total_w < 1e-15:",
			"            # リセット / 均等化 (過度な縮退防止)",
			"            self.weights = [1.0 / self.num_particles] * self.num_particles",
			"        else:",
			"            inv_total = 1.0 / total_w",
			"            self.weights = [w * inv_total for w in weights]",
			"",
			"    def resample(self):",
			"        \"\"\"系統的リサンプリング (Systematic Resampling O(M))\"\"\"",
			"        m = self.num_particles",
			"        weights = self.weights",
			"        particles = self.particles",
			"",
			"        # 有効粒子数のチェック (縮退判定)",
			"        n_eff = 1.0 / sum(w * w for w in weights)",
			"        if n_eff > m * 0.5:",
			"            return  # 縮退していなければリサンプリングをスキップ",
			"",
			"        new_particles = []",
			"        step = 1.0 / m",
			"        r = random.uniform(0, step)",
			"        acc = weights[0]",
			"        idx = 0",
			"",
			"        for j in range(m):",
			"            target = r + j * step",
			"            while target > acc and idx < m - 1:",
			"                idx += 1",
			"                acc += weights[idx]",
			"            new_particles.append(particles[idx])",
			"",
			"        self.particles = new_particles",
			"        self.weights = [step] * m",
			"",
			"    def estimate_best(self):",
			"        \"\"\"最大重みを持つパーティクルを推定値として取得\"\"\"",
			"        best_idx = max(range(self.num_particles), key=lambda i: self.weights[i])",
			"        return self.particles[best_idx]"
		],
		"description": "AHC Particle Filter for State Tracking with Systematic Resampling for PyPy"
	}
}